# D8 深度讲解：Windows Server 入门与静态 IP 排障的每一个细节

> 配套：行动指南 D8（装 Windows Server 2022 + 静态 IP）。
> 本日核心：**你从 Linux 世界跨进了 Windows 服务器世界**，并亲手踩掉了 VMware 网络里最经典的一个坑。

---

## 开篇：D8 在干什么

一句话：**把第二台服务器（Windows）加入你的实验网络**，为域控（AD）打底。

到 D8 为止，你的实验环境长这样：

```
宿主笔记本（16GB，VMware Workstation）
  ├─ sv01  ：Ubuntu Server 24.04   → 192.168.207.128（W1 交付的 Linux 服务器）
  └─ DC01  ：Windows Server 2022   → 192.168.207.10 （今天的 Windows 服务器，未来的域控）
```

**为什么要加一台 Windows**：石家庄政企/中小企业的 IT 环境，**Windows Server 占比极高**（域控、文件服务器、内网系统都在 Windows 上）——只会 Linux 的运维在这个市场是残缺的。

---

## 第一部分：版本选择——Standard vs Datacenter

### 两者几乎一样，差别只在授权

| 维度 | Standard（标准版） | Datacenter（数据中心版） |
|---|---|---|
| **虚拟化授权** | 允许 **2 个**虚拟机 | **无限**个虚拟机 |
| AD 域控 / GPO / DNS / DHCP | ✅ 完全一样 | ✅ 完全一样 |
| 独有高级功能 | — | 存储副本、软件定义网络、屏蔽 VM |
| 价格 | 几千元 | 几万元 |
| 典型场景 | 中小企业物理机/小规模虚拟化 | 数据中心、私有云 |

**你选 Standard 的三个理由**：
1. 要学的 AD/GPO/DNS 两版操作**完全一致**
2. 你投的政企驻场/中小企业岗，公司里 **95% 用的是 Standard**
3. 你只跑一台 DC01，用不到"无限虚拟机"授权

> **面试加分点**："Standard 只能授权 2 个虚拟机、Datacenter 无限"——很多干了几年的运维都答不上这个授权区别，因为中小企业根本用不到 Datacenter。

## 第二部分：为什么 Windows 要选 Desktop Experience（和 Linux 恰好相反）

安装时四个选项：

```
Windows Server 2022 Standard                                    ← Server Core（纯命令行）
Windows Server 2022 Standard (Desktop Experience)               ← ✅ 你选的
Windows Server 2022 Datacenter                                  ← Server Core
Windows Server 2022 Datacenter (Desktop Experience)
```

| | 带 Desktop Experience | 不带（Server Core） |
|---|---|---|
| 界面 | 图形界面 | 纯命令行/远程 PowerShell |
| AD 管理工具 | **服务器管理器、AD 用户和计算机、组策略管理**——全是图形工具 | 要用命令行/远程工具管理 |
| 适合谁 | 新手、多数企业 | 资深运维、追求最小攻击面的企业 |

**为什么和 Linux 的选择相反**：
- Linux 服务器管理 **90% 靠命令行**（所以选无桌面的 Server 版）
- Windows 服务器管理 **90% 靠图形工具**（AD 的管理工具全是 GUI，用命令行配 AD 对新手是灾难）
- **跟着管理方式选版本**，而不是教条地"服务器就该无桌面"

---

## 第三部分：静态 IP 排障（今天最大的坑，也是最值钱的课）

### 3.1 现象回顾

| 操作 | 结果 |
|---|---|
| 按手册填 `192.168.10.10`，不填网关 | 上不了网 |
| 改 `192.168.207.11` + 网关 `192.168.207.1` | 还是 ping 不通 |
| 切回 DHCP 自动获取 → `ipconfig /all` | **网关是 192.168.207.2**（水落石出） |
| 静态改 `192.168.207.10` + 网关 `.2` + DNS `.2` | ✅ 通了 |

### 3.2 根因：`.1` 和 `.2` 的身份

| 地址 | 真实身份 | 能当网关吗 |
|---|---|---|
| `192.168.207.1` | **宿主机的 VMnet8 虚拟网卡**（VMware 在宿主机上装的一块网卡） | ❌ 它只是一块"网卡"，不做转发、不响应 ICMP |
| `192.168.207.2` | **VMware NAT 设备**（真正的路由器，帮虚拟机转发流量） | ✅ 这才是网关 |
| `192.168.207.128~254` | **DHCP 自动分配池**（sv01=.128、DC01 曾=.129） | — |

**为什么会填错 `.1`**：大多数人的直觉是"网关就是网段的第一个地址（.1）"——在普通路由器环境这个直觉是对的，**但 VMware NAT 的网关固定是网段的第二个地址（.2）**，`.1` 被宿主机的虚拟网卡占了。

### 3.3 由此推出的静态 IP 选址规则（记住，终身受用）

1. **网关填 `.2`**（VMware NAT 环境的铁律）
2. **静态 IP 选 DHCP 池之外的地址**——DHCP 池是 `.128~.254`，所以 `.10`、`.11` 永远不会和 DHCP 分配的地址冲突
3. **改完立刻双重验证**：`ping 网关`（内网通）+ `ping 8.8.8.8`（外网通）

### 3.4 域控为什么必须静态 IP（再强调一遍）

**域控 = 全网的"地址簿"和"门卫"**：
- 所有加域电脑靠它解析域名（DNS）
- 所有加域电脑靠它验证身份（AD）

**它的 IP 一旦变化**（DHCP 租约 30 分钟就换一次）：
- 加域电脑登录失败（找不到域）
- DNS 解析全挂
- 生产环境 = 重大事故

**所以域控的静态 IP 是硬性要求，不是"建议"。**（你的租约只有 30 分钟——半小时后 IP 就可能变，这就是最好的反面教材。）

---

## 第四部分：DC01 机器名的意义

**为什么改名为 DC01**：
- `DC` = Domain Controller（域控制器）——一眼看出这台机器的角色
- `01` = 第一台域控（企业里会有 DC01、DC02 做冗余）
- **改名必须在升域之前**——升域后机器名会被写进 AD 数据库，再改名是"大手术"（要先降级、改名、再升级）

**企业命名规范举例**：
- 按角色：`DC01`（域控）、`FS01`（文件服务器）、`WEB01`
- 按环境：`PROD-DC01`、`TEST-DC01`

**你现在的两台机器**：
- `sv01` = Linux 服务器（W1 交付）
- `DC01` = Windows 域控（W2 主角）

---

## 第五部分：三台机器的拓扑（跨机器操作的意识）

```
┌─ 主机（Administrator，WorkBuddy 在这里）──────────┐
│  │ 远程控制                                        │
│  ▼                                                │
│  笔记本（Jasper，VMware 宿主，PowerShell 在这里）    │
│    ├─ VMware ─ sv01  192.168.207.128  Ubuntu       │
│    └─ VMware ─ DC01  192.168.207.10   Windows      │
└───────────────────────────────────────────────────┘
```

**今晚踩的最后一个坑就是它**：README 文件在**主机**上，而你的 PowerShell 在**笔记本**上——跨机器操作时，**先想清楚"文件在哪台机器、命令要在哪台机器执行"**。

**以后的三层意识**：
| 问题 | 想清楚 |
|---|---|
| 文件在哪台机器 | 主机？笔记本？sv01？DC01？ |
| 命令在哪执行 | 宿主 PowerShell？DC01 的 PowerShell？sv01 的 SSH？ |
| 结果在哪验证 | 文件传过去后要在目标机器上验证 |

---

## 第六部分：面试话术 + 追问预案

### 60 秒话术

> "我的实验环境是两台虚拟机：一台 Ubuntu Server 做 Linux 交付练习，一台 Windows Server 2022 Standard 准备做 AD 域控。配 Windows 静态 IP 时踩了一个 VMware 的经典坑——我以为网关是网段的 .1，实际 VMware NAT 的网关是 .2，.1 是宿主机的虚拟网卡，不响应也不转发。我通过切回 DHCP 用 ipconfig /all 看实际下发的网关才确认，改成 .2 后网络恢复。另外我确认了 DHCP 池是 128 到 254，所以静态 IP 选了 .10，永远不会有 IP 冲突。域控用静态 IP 是硬要求——它租约才 30 分钟，IP 一变所有加域机器都找不到它。"

### 追问预案

| 追问 | 答 |
|---|---|
| Standard 和 Datacenter 区别 | 授权虚拟机 2 个 vs 无限；AD/GPO/DNS 功能一样；中小企业用 Standard |
| Desktop Experience 和 Server Core | 带图形 vs 纯命令行；AD 管理工具是图形的，学习阶段选桌面版 |
| 域控为什么必须静态 IP | 域控是 DNS + 身份验证中心，IP 变了全网加域机器都失效；租约 30 分钟就会变 |
| VMware NAT 网关为什么是 .2 | .1 是宿主机的 VMnet8 虚拟网卡，.2 才是 NAT 转发设备（实测 ipconfig /all 确认） |
| 怎么验证 IP 配置生效 | ping 网关（内网通）+ ping 8.8.8.8（外网通）双验证 |

---

## 附录：D8 操作速查表

| 操作 | 路径/命令 |
|---|---|
| 改机器名 | 服务器管理器 → 本地服务器 → 计算机名 → 更改 |
| 配静态 IP | 控制面板 → 网络和共享中心 → 更改适配器设置 → Ethernet0 → IPv4 属性 |
| 查完整网络配置 | `ipconfig /all` |
| 测试内网 | `ping 192.168.207.2`（网关）、`ping 192.168.207.128`（sv01） |
| 测试外网 | `ping 8.8.8.8` |
| 验证 DNS | `nslookup baidu.com` |
| 关机 | `shutdown /s /t 0`（或图形关机） |
| 重启 | `shutdown /r /t 0` |

---

*到 D8 为止，你的实验环境是双服务器（Linux + Windows）的混合网络——这正是石家庄政企/中小企业 IT 的真实形态。D9 把 DC01 升级为域控制器，整个实验网络的"中央大脑"就诞生了。*
