# D9 深度讲解：域控上线的每一个细节

> 配套：行动指南 D9（安装 AD DS 并提升为域控制器）。全部解读基于你的真实操作与 nslookup 输出。
> D9 的本质：**DC01 从"一台普通服务器"升级为"整个实验网络的大脑"**——从今天起，账号、密码、权限、策略都由它统一管理。

---

## 开篇：域控上线意味着什么

D9 之前，你的两台机器（sv01 / DC01）是**各自为政**的——各有各的账号，互不相干。

D9 之后，出现了一个**中央权威**：

| 之前（工作组模式） | 之后（域模式） |
|---|---|
| 每台机器各有各的账号 | **一套账号全网通用**（建一次 zhang.san，域内所有电脑都能登录） |
| 密码策略各管各的 | 域控统一下发 |
| 权限逐台配置 | 组策略批量下发 |
| 电脑之间互不信任 | 域内机器**自动互信**（ Kerberos 认证） |

**这就是企业 IT 集中管理的核心**——石家庄政企/中小企业的内网，几乎全是这个模式。你今天搭的就是它们的"大脑"。

---

## 第一部分：五个词的大白话详解

| 词 | 大白话 | 关键点 |
|---|---|---|
| **域（Domain）** | 一个"组织"：所有电脑加入后用**同一套账号密码** | 类比：小区门禁——办一张卡进所有楼 |
| **域控制器（DC）** | 管理"组织"的大脑服务器：**存账号、验密码、发策略** | 你的 DC01 现在就是 |
| **AD DS** | Active Directory 域服务——微软"域"的技术实现 | 你装的这个"角色" |
| **林（Forest）** | 最大的管理边界（一个林可含多个域） | 你的实验只有一个域，**林=域** |
| **sjzlab.local** | 你域的名字 | 类比公司内网域名 |

**DSRM 密码（单独强调）**：
- 全称 Directory Services Restore Mode（目录服务还原模式）
- **域控的"备用钥匙"**：AD 数据库损坏、域控无法正常启动时，用它进修复模式抢救
- **它和平时登录的管理员密码是两回事**——丢了就真的进不去修复模式了
- 你设的是 `Qwer1234!`——**记到你的密码管理文档里，这是生产级的重要凭据**

---

## 第二部分：升域流程的每一步在干什么

### 步骤"添加新林 → sjzlab.local"

你在创建一个全新的 AD 环境（林）：
- **新林** = 从零开始建一个管理边界（而不是加入已有的域）
- **sjzlab.local** = 这个域的名字（DNS 命名）
- 从这一刻起，DC01 成为 **sjzlab.local 域的第一台域控制器**

### 安装时自动附带的东西（你可能没注意）

勾选提升时，系统**自动帮你装了并配置了**：
1. **DNS 服务器**（AD 强依赖 DNS——所有电脑靠 DNS 找到域控）
2. **AD 数据库**（NTDS.dit，存在 `C:\Windows\NTDS\`）
3. **SYSVOL** 共享目录（组策略的存放地，`C:\Windows\SYSVOL`）
4. **Kerberos 认证服务**（域内的"验票"协议）

### DSRM 密码输入的那一步

这是给 AD **数据库**设的应急密码——将来域控坏了进"目录服务还原模式"时用的。**平时用不到，但丢了就是灾难**（没有它，AD 坏了救不回来）。

### 重启后登录界面变化

登录用户名可能显示 `SJZLAB\Administrator`——**SJZLAB 是 NetBIOS 名**（域的旧式短名，自动从 sjzlab.local 取的）。两种登录写法等价：
- `SJZLAB\Administrator`
- `administrator@sjzlab.local`

---

## 第三部分：DNS 与 AD 的强绑定（为什么升域后必须改 DNS）

**AD 的工作方式**：域里所有电脑要登录，第一步是**通过 DNS 找到域控在哪**（查询 `_ldap._tcp.dc._msdcs.sjzlab.local` 这类 SRV 记录）。

**所以**：
1. 装 AD 时**必须同时有 DNS**（所以系统自动装了）
2. AD 会**把自己的域名记录写进 DNS**（你 nslookup 看到的 sjzlab.local → 192.168.207.10 就是这个记录）
3. **域控自己的 DNS 必须指向自己**——否则它解析自己的域名都要问别人（鸡生蛋问题）

**这就是 D9 收尾必做项"DNS 改 192.168.207.10"的原因**——不改的话，DC01 解析自己域名要走 NAT 网关的 DNS 转发，**慢且不可靠**，D10 加域时会出各种诡异问题。

---

## 第四部分：你的 nslookup 输出解读（三段拆开）

```
1.0.0.0.0...0.0.0.0.ip6.arpa                       ← ① 噪音
    primary name server = 1.0.0.0...ip6.arpa        ← ① 噪音
    serial = 0 / refresh / retry / expire / TTL      ← ① 噪音
服务器:  UnKnown
Address:  ::1                                       ← ② 解释
名称:    sjzlab.local                                ← ③ 核心证据
Address:  192.168.207.10                            ← ③ 核心证据
```

| 段 | 是什么 | 判定 |
|---|---|---|
| ① `ip6.arpa` 那一大串 | **IPv6 反向解析区域的参数**（nslookup 查询 IPv6 反向记录时的默认输出） | 正常噪音，不是错误 |
| ② `服务器 ::1` | 查询走的 DNS 服务器是 **IPv6 回环地址**（本机就是 DNS，回环也指向自己） | 正常；建议确认 IPv4 DNS 已设为 192.168.207.10 |
| ③ `sjzlab.local → 192.168.207.10` | **AD 的 DNS 正常工作**：域名 → 域控 IP | ⭐ 这就是 D9 成功的核心证据 |

**注意一个细节**：nslookup 显示 DNS 服务器为 `::1`——可能你的 DNS 列表里只改了 IPv4 或 IPv6 优先。**建议确认 IPv4 的首选 DNS 已经是 192.168.207.10**（如果还是 192.168.207.2，赶紧改——D10 加域全靠它）。

---

## 第五部分：现实图景（企业域控的真面目）

| 你的实验 | 企业级 |
|---|---|
| 1 台 DC01 | **至少 2 台域控做冗余**（DC01+DC02，一台挂了另一台顶上） |
| 单网段 207.x | 多站点、多子网、站点间复制 |
| 本地管理员管一切 | **分层委派**：帮助台只能重置密码、运维只能管服务器 OU |
| 无外部依赖 | 和防火墙/VPN/证书服务联动 |
| 手动建用户 | 批量脚本/HR 系统自动同步 |

**你投的政企驻场岗**：域控值班是日常——**重置密码、解锁账号、加电脑进域、排查组策略**就是每天的主要工作。你现在练的就是它们的地基。

---

## 第六部分：面试话术 + 追问预案

### 60 秒话术

> "我在实验环境搭了一台 Windows Server 域控制器。先把网络配成静态 IP——这里踩过一个 VMware 的坑：我以为网关是网段的 .1，实际 VMware NAT 的网关是 .2，.1 是宿主机的虚拟网卡，不转发流量。我是通过切回 DHCP 看实际下发的网关才发现的。
>
> 然后装 AD DS 角色并提升为域控制器，创建了新林 sjzlab.local。装的过程中理解了 AD 和 DNS 是强绑定的——域内电脑靠 DNS 的 SRV 记录找域控，所以装 AD 自动带 DNS。升完域我把 DNS 指向改成域控自己，并用 nslookup 验证 sjzlab.local 能解析到域控 IP，确认 AD 的 DNS 正常工作。
>
> 另外记住了 DSRM 密码的重要性——它是域数据库损坏时进还原模式的唯一钥匙，和管理员密码是两回事。"

### 追问预案

| 追问 | 答 |
|---|---|
| 域和Workgroup区别 | 工作组：每台电脑各自管账号（分散）；域：统一账号中心（集中管理 + 一次登录全网通行） |
| 什么是林 | 最大的安全/管理边界；单域环境里林=域 |
| AD 为什么依赖 DNS | 域内电脑通过 DNS 的 SRV 记录定位域控；DNS 挂了=全员无法登录 |
| DSRM 密码是什么 | 目录服务还原模式密码——AD 数据库出问题时进修复模式用；和管理员密码是两回事 |
| 为什么域控不能 DHCP | 域控是 DNS+认证中心，IP 变了全网失效；生产必须静态或 DHCP 保留 |
| 一台域控够吗 | 生产至少两台做冗余（AD 数据自动复制，一台挂另一台顶） |
| 什么是 OU | 域内的组织容器（类比部门文件夹），用于分类管理用户/电脑和精准下发组策略 |

---

## 附录：D9 命令/操作速查表

| 操作 | 路径/命令 |
|---|---|
| 装 AD DS 角色 | 服务器管理器 → 添加角色和功能 → Active Directory 域服务 |
| 提升为域控 | 安装完成页的链接 / 服务器管理器黄色旗子 |
| 新建林 | 部署配置 → 添加新林 → 根域名 |
| DSRM 密码 | 域控制器选项页设置（备用钥匙，单独记） |
| 域登录 | `SJZLAB\Administrator` 或 `administrator@sjzlab.local` |
| 改 DNS 指向自己 | IPv4 属性 → 首选 DNS = 192.168.207.10 |
| 验证域名解析 | `nslookup sjzlab.local` |
| 验证域控状态 | 服务器管理器左侧 AD DS / DNS 角色齐全 |
| 打开 AD 管理工具 | 服务器管理器 → 工具 → Active Directory 用户和计算机 |
| 组策略工具 | 服务器管理器 → 工具 → 组策略管理 |

---

*D9 完成：你的实验网络有了"大脑"。D10 会给这个大脑装上"第一套组织架构"——建 OU、建域用户、下发第一条组策略，让 AD 的集中管理能力第一次真实展现。*
