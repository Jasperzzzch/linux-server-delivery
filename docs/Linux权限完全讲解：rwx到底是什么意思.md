# Linux 权限完全讲解：rwx 到底是什么意思

> 针对"x 到底是什么"的困惑重写，覆盖权限的全部知识点。所有解释都配可验证的例子，你可以边看边在自己的 sv01 上试。

---

## 一、权限为什么存在（先解决"为什么"）

Linux 是多用户系统——同一台机器上可能有几十个用户、上百个进程在跑。权限解决三个问题：

| 问题 | 权限怎么解决 |
|---|---|
| 我的文件不想被别人看 | 权限位控制谁能读 |
| 我的文件不想被别人改 | 权限位控制谁能写 |
| 系统文件不能被普通用户删 | 权限位控制谁能写目录 |

**核心原则：最小权限**——只给完成工作所必需的最小权限，多一分都不给。这是安全的基础，也是等保、ISO27001 的硬性要求。

---

## 二、三组身份：u / g / o

每个文件都绑定**三组权限位**，分别对应三类人：

```
-rwx r-x ---
 └──┘ └─┘ └┘
  │    │   └── others（其他人）：既不是属主，也不在属组
  │    └────── group（属组）：和文件属组同组的用户
  └─────────── user（属主）：文件的所有者
```

### ⚠️ 一个经典陷阱：权限检查是"先匹配先停止"

**Linux 检查权限时，按顺序匹配，命中一次就停止，不会叠加**：

1. 你是文件的**属主**吗？→ 是，就用**属主那三位**，到此结束
2. 不是属主 → 你在**属组**里吗？→ 是，就用**属组那三位**，到此结束
3. 都不是 → 用**其他那三位**

**这意味着**：如果你是属主，但属主位是 `---`，那么即使属组位是 `rwx`，你也什么都做不了！

举例：
```
--- rwx rwx   jasper jasper  test.txt
```
jasper 是这个文件的属主 → 匹配属主位 `---` → **jasper 自己反而读不了自己的文件**（虽然其他人的权限位是 rwx）。

> 实际排障中，"我是属主为什么还报 Permission denied" 往往就是这么来的。

---

## 三、x 到底是什么（核心困惑点）

**x = execute，中文常译作"执行"——但这个翻译对目录来说是误导。**

准确理解：**x 是"能不能把它用起来"的权限**，具体含义取决于对象是文件还是目录。

### 3.1 x 对文件：能不能当程序跑

| 权限 | 表现 |
|---|---|
| 有 x | 可以 `./脚本.sh` 直接运行 |
| 无 x | 即使脚本内容完全正确，运行就报 `Permission denied: ./脚本.sh` |

**关键：r 和 x 是独立的**

| 组合 | 效果 | 例子 |
|---|---|---|
| 有 r 无 x | 能看内容，**不能运行** | 刚写的脚本忘了 chmod +x |
| 有 x 无 r | **能运行，不能看内容** | 二进制程序（你能用，但看不到源码） |
| 都有 | 能看能跑 | Shell/Python 脚本（**必须 r+x**，因为解释器要读内容） |

> **为什么脚本必须 r+x 而二进制只要 x**：
> 脚本（bash/python）需要解释器**读入文件内容**再执行，所以必须能读。
> 二进制是 CPU 直接执行的机器码，只需加载，**不需要"读"这个动作**。

### 3.2 x 对目录：能不能"穿过"它（重点）

**目录不能"执行"，所以这里的 x 实际含义是"搜索/穿过"（search / traverse）**——能不能穿过这个目录去访问它里面的东西。

看三个组合（建议你立刻在 sv01 上验证，比看文字直观十倍）：

#### 情况 A：只有 r，没有 x（`r--`）

```
能：  ls 目录名        → 看到里面的文件名列表
不能：cd 目录名        → Permission denied
不能：cat 目录/文件    → Permission denied
```

**比喻**：你能看到门外的**名单**（有哪些人住这），但**进不了门**，也接触不到里面任何人。

#### 情况 B：只有 x，没有 r（`--x`）

```
能：  cd 目录名              → 能进去
能：  cat 目录/已知的文件名  → 能读（前提是你已经知道文件名）
不能：ls 目录名              → Permission denied（列不出内容）
```

**比喻**：一个**漆黑的房间**——你能进去，如果你**事先知道东西放在哪**，能摸到它；但你**看不见房间里到底有什么**（不能 ls）。

#### 情况 C：r + x 都有（`r-x`）← 目录的标准配置

```
能：ls 列出内容、cd 进入、读文件 —— 全部正常
```

**这就是你在 755 / 750 里看到的那个 `5`**（r=4 + x=1 = 5）。

### 3.3 为什么目录必须有 x（路径解析的角度）

访问 `/home/jasper/test.txt` 时，系统要**依次穿过**每一级目录：

```
/  →  home  →  jasper  →  test.txt
 ✗      ✗        ✗
      每一级都必须有 x（穿过权限）
```

**任何一级缺 x，整条路径就断**，哪怕最终文件的权限是 777 也访问不到。

> 这解释了一个常见故障：文件权限没问题，但就是打不开——八成是**上级目录缺 x**。

---

## 四、r / w / x 完整对照表

| 权限 | 对**文件** | 对**目录** |
|---|---|---|
| **r** (读, 4) | 能看内容（`cat`、`vim`） | 能列出里面有什么（`ls`） |
| **w** (写, 2) | 能修改内容 | 能**新建/删除/重命名**里面的文件 |
| **x** (执行, 1) | 能当程序跑 | 能**穿过/进入**它（`cd`、访问内部文件） |

**关于目录的 w，有个反直觉的点**：
- 删除一个文件，**不需要对这个文件有写权限**，只需要对**它所在的目录有 w 权限**
- 所以 `/tmp` 是 1777（所有人可写），但里面别人的文件你不能改——因为**改文件内容需要文件本身的 w**，而**删文件需要目录的 w**

---

## 五、文件类型位（权限串的第一个字符）

| 字符 | 含义 | 你在哪见过 |
|---|---|---|
| `-` | 普通文件 | `-rw-rw-r--`（你的 test.txt） |
| `d` | 目录 | `drwxr-x---`（你的家目录） |
| `l` | **软链接** | `lrwxrwxrwx`（你的 softlink.txt） |
| `c` | 字符设备 | 键盘、终端 `/dev/tty` |
| `b` | 块设备 | 硬盘 `/dev/sda` |
| `s` | 套接字 | 进程通信 |
| `p` | 管道 | 进程通信 |

---

## 六、数字表示法：r=4, w=2, x=1

一组内相加，三组拼起来：

```
rwx = 4+2+1 = 7
r-x = 4+0+1 = 5
rw- = 4+2+0 = 6
r-- = 4+0+0 = 4
--- = 0+0+0 = 0
```

**755** = 属主 7(rwx) + 属组 5(r-x) + 其他 5(r-x)
**750** = 属主 7(rwx) + 属组 5(r-x) + 其他 0(---)

### 常见组合速查

| 数字 | 权限串 | 用在哪 | 为什么 |
|---|---|---|---|
| **755** | rwxr-xr-x | 网站目录、程序 | 其他人要能进、能读 |
| **750** | rwxr-x--- | **你家目录**（24.04 默认） | 其他人完全进不去 |
| **644** | rw-r--r-- | 配置文件、文档 | 别人可读，只有属主能改 |
| **700** | rwx------ | `~/.ssh` | 只有自己能进 |
| **600** | rw------- | 私钥 `id_ed25519` | 只有自己能读写 |
| **1777** | rwxrwxrwt | `/tmp` | 所有人可写，但只能删自己的（sticky 位） |

---

## 七、umask：新建文件的权限从哪来（解释你的 664）

你今天 `echo "运维实验" > test.txt` 建的文件是 **`-rw-rw-r--`（664）**，不是 644——为什么？

**答案：umask（权限掩码）**。

```
新建文件的默认权限 = 666 - umask
新建目录的默认权限 = 777 - umask
```

| umask | 新建文件 | 新建目录 |
|---|---|---|
| 022 | 644 (rw-r--r--) | 755 |
| **002** | **664 (rw-rw-r--)** | **775** |

Ubuntu 默认 **umask 002**（每个用户有自己同名的组，所以放开组权限无害）——这就是你的文件是 664 的原因。

查看当前 umask：
```bash
umask
```

> 面试能讲出"文件默认权限由 umask 决定"，比只会 chmod 高一个层次。

---

## 八、改权限的命令

### chmod（改权限）

```bash
chmod 755 文件名              # 数字法：直接设成 755
chmod u+x 文件名              # 符号法：给属主加执行权限
chmod g-w 文件名              # 给属组去掉写权限
chmod o+r 文件名              # 给其他人加读权限
chmod a=r 文件名              # 所有人(a)都设成只读(r)
chmod -R 755 目录名           # 递归改（谨慎！）
```

**符号法字母**：`u`=属主，`g`=属组，`o`=其他，`a`=全部；`+`加、`-`减、`=`设为。

> ⚠️ **运维红线**：永远不要 `chmod -R 777`。这等于把系统敞开，是新手最危险的操作。

### chown / chgrp（改属主、属组）

```bash
sudo chown ops:ops 文件名           # 同时改属主和属组
sudo chown ops 文件名               # 只改属主
sudo chgrp ops 文件名               # 只改属组
sudo chown -R ops:ops 目录名        # 递归改（新建账号后常用）
```

---

## 九、特殊权限（进阶，了解即可）

权限串里除了 rwx，偶尔还会看到 `s` 和 `t`：

| 特殊位 | 数字 | 作用 | 例子 |
|---|---|---|---|
| **suid** (s) | 4000 | 执行时**临时获得属主身份** | `/usr/bin/passwd`（普通用户改自己的密码需要写 /etc/shadow） |
| **sgid** (s) | 2000 | 目录中新建文件**自动继承属组** | 团队共享目录 |
| **sticky** (t) | 1000 | 目录里**只能删自己的文件** | `/tmp`（1777，所有人可写但不能互删） |

**为什么 `/tmp` 是 1777**：所有人都要在里面建临时文件（777），但不能删别人的（sticky 位）。

---

## 十、现实案例串讲（把这些连起来）

| 场景 | 权限 | 原因 |
|---|---|---|
| `~/.ssh` 目录 | 700 | 别人不能进你的 .ssh |
| `authorized_keys` | 600 | **别人能改 = 能塞自己的公钥 = 能登录你的账号** |
| `id_ed25519` 私钥 | 600 | 泄露 = 身份被盗用 |
| 家目录（24.04） | 750 | 别人进不去（隐私保护） |
| 网站目录 `/var/www/html` | 755 | web 服务（www-data 用户）要能读能进 |
| 日志目录 | 755 或 775 | 运维要能读，服务要能写 |

**回到 D2 的那个"洁癖"**：OpenSSH 强制检查 .ssh 和 authorized_keys 的权限，不合规就拒绝密钥登录——**现在你能理解这不是 bug，是纵深防御**。

---

## 十一、软链接 vs Windows 快捷方式

**答：是的，概念几乎完全相同。但实现层级不同，所以软链接更强。**

### 相同点（这就是"相通"的部分）

| 特性 | Windows 快捷方式 | Linux 软链接 |
|---|---|---|
| 本质 | 存的是**目标路径** | 存的是**目标路径** |
| 删了目标 | 双击报错"找不到" | `cat` 报 No such file |
| 能跨分区 | 能（指向 D 盘） | 能（跨文件系统） |
| 能指向目录 | 能 | 能 |
| 删掉它 | 不影响原文件 | 不影响原文件 |

### 关键区别（实现层级不同）

| | Windows 快捷方式 | Linux 软链接 |
|---|---|---|
| **本质** | **一个普通文件**（.lnk），由资源管理器"解释" | **文件系统层的机制**，内核直接支持 |
| **对程序透明吗** | ❌ 不透明：程序打开 .lnk 看到的是二进制内容，必须 Shell 来解释 | ✅ **完全透明**：程序 `open()` 软链接时，内核自动转到目标，**程序根本不知道自己在读链接** |
| **命令行能用吗** | cmd 里不能直接 cd 进快捷方式 | 命令行完全可用（`cd`、`cat`、`vim` 都行） |
| **大小** | 几百字节~几 KB（含图标、描述等） | **就是路径字符串的字节数**（你的 softlink 是 21 字节 = `/home/jasper/test.txt` 的长度） |

### 一句话总结

> **软链接 ≈ 快捷方式，但 Linux 的软链接是"操作系统内核认的快捷方式"，Windows 快捷方式是"资源管理器认的快捷方式"。** 前者对任何程序都生效，后者只有图形界面（或支持 .lnk 的程序）才认。

### 补充：Windows 其实也有"真链接"

NTFS 文件系统其实支持硬链接和符号链接，只是普通用户不知道：

```cmd
mklink /H 链接名 目标      # 硬链接（同 Linux 硬链接，同一份数据多个名字）
mklink /D 链接名 目标      # 目录符号链接
mklink /J 链接名 目标      # 目录联接（junction）
```

平时大家说的"快捷方式"是右键创建的 `.lnk`，那是**应用层**的东西，和 `mklink` 创建的**文件系统层**链接不是一回事。

> **面试说法**：先说"可以类比快捷方式"，再补一句"不过它是文件系统层实现的、对程序透明"——既好懂又显得专业。

---

## 十二、面试考点清单

1. **rwx 对文件和目录的区别**（目录的 x = 能否穿过/进入）⭐ 必考
2. **755 / 750 / 644 / 600 各是什么、用在哪**
3. **目录的 r 和 x 单独存在时各能做什么**（黑房间比喻）⭐ 拉分题
4. **权限检查顺序**（先匹配先停止，属主位为空则属组位再高也没用）⭐ 陷阱题
5. **umask 决定新建文件的默认权限**
6. **软硬链接区别 + 与 Windows 快捷方式的类比**
7. **删文件需要目录的 w，改文件需要文件本身的 w**

---

## 附：动手验证清单（建议做一遍，比看十遍强）

```bash
mkdir ~/perm-test                    # 建测试目录
cd ~/perm-test
echo "secret" > file.txt

# 实验1：目录只有 r（去掉 x）
chmod 444 ~/perm-test
ls ~/perm-test                       # 能列出 file.txt
cat ~/perm-test/file.txt             # Permission denied ✓

# 实验2：目录只有 x（去掉 r）
chmod 111 ~/perm-test
ls ~/perm-test                       # Permission denied ✓
cat ~/perm-test/file.txt             # 能读！（因为你知道文件名）✓

# 实验3：恢复正常
chmod 755 ~/perm-test
ls ~/perm-test && cat ~/perm-test/file.txt    # 都正常 ✓

# 实验4：文件没有 x 就跑不起来
echo '#!/bin/bash
echo hello' > ~/perm-test/s.sh
chmod 644 ~/perm-test/s.sh
~/perm-test/s.sh                     # Permission denied（有 r 无 x）✓
chmod 755 ~/perm-test/s.sh
~/perm-test/s.sh                     # 输出 hello ✓

# 清理
rm -rf ~/perm-test
```

**做完这组实验，你对 x 的理解就不再是"背下来的"，而是"亲眼看到的"。**
