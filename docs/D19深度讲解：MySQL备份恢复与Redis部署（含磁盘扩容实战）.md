# D19 深度讲解：MySQL 备份恢复与 Redis 部署（含磁盘扩容实战）

> 日期：2026-09-06（09:40 – 10:30）｜环境：VMware NAT `192.168.207.0/24`
> 参与机器：**sv01**（`192.168.207.128`，Ubuntu 24.04.4，LVM 磁盘 20G→**60G**）
> 版本：MySQL **8.0.46** / Redis **7.x**（Ubuntu 24.04 官方源）
> 状态：**备份→删库→恢复全链路通过；五种数据类型全实操；RDB/AOF 配置确认**
> 为什么全程在 sv01 做：sv02 的 MySQL 跑着 Zabbix 全部数据（203 张表），删库演练绝不能在那台机器上练。

---

## 开篇：先想明白这两样东西为什么存在

### 备份恢复：运维的生命线

想象一下：周五下午 5 点，你敲了一行 `DROP DATABASE orders;`——几百万订单数据瞬间蒸发。**没有备份，你的职业生涯可能和这行数据一起结束。**

备份恢复的意义不是"防止出事"——**出事是必然的**（人为误删、硬件损坏、软件 bug、勒索病毒），它的意义是"**出事之后能回来**"。

而且有一条铁律贯穿整个行业：

> **没验证过恢复的备份，等于没有备份。**
> 备份文件躺在那里不代表能用——可能是坏的、可能恢复步骤没走通、可能恢复出来缺数据。所以今天的演练不是"走过场"，而是把"备份"从一句口号变成一条验证过的通路。

### Redis：给 MySQL 减负的"内存层"

先看没有缓存的架构有多难受：

```
没有缓存：用户请求 ──► 应用 ──► MySQL（读磁盘）──► 返回
                              ↑ 每个请求都碰磁盘，MySQL 读写能力有天花板（几千 QPS 级别）

有缓存：  用户请求 ──► 应用 ──► Redis（读内存，微秒级）── 命中 ──► 返回
                              └─ 未命中 ──► MySQL ──► 写回 Redis
                              ↑ 80-95% 的请求被 Redis 拦下，MySQL 压力骤降
```

**Redis = 数据放内存里的数据库**。内存读写是纳秒~微秒级，磁盘是毫秒级，差了 1000 倍。所以它天生适合干"**又快又热**"的活：缓存、排行榜、会话存储、消息队列。

**MySQL 和 Redis 不是竞争关系，是搭档**：MySQL 负责"数据不能丢"（持久化存储），Redis 负责"数据要快读"（内存缓存）。业界叫它 **cache-aside 架构**，是后端系统的标准配置。

---

## 0. 这一天的产出总览

| # | 产出 | 验证方式 |
|---|---|---|
| 1 | **sv01 磁盘扩容 20G→60G**（LVM 在线四步） | `df -h /` = 58G 总 / 49G 可用 / 11% |
| 2 | MySQL 8.0.46 部署 + 开机自启 | `is-active` = active；`SELECT VERSION()` |
| 3 | 演练数据 `test.employees`（3 行） | `SELECT *` 可见 |
| 4 | 备份专用账号 `bk`（不用 root） | `mysql -u bk -p'...'` 登录成功 |
| 5 | 全量备份 1.3M + 单库备份 2.2K | `ls -lh` + `grep CREATE TABLE` |
| 6 | **删库 → 恢复 → 数据完整回来** | `SELECT * FROM test.employees` 3 行 |
| 7 | Redis 部署 + 开机自启 | `redis-cli ping` = PONG |
| 8 | 五种数据类型全实操 | string/hash/list/set/zset 全通 |
| 9 | `--bigkeys` 大 key 扫描 | 五种类型最大 key 全部列出 |
| 10 | RDB/AOF 持久化配置确认 | `config get save` / `appendonly` |

---

## 1. 概念地基

### ① 备份文件的本质：一坨能重放的 SQL

今天你在 `grep -A2 "CREATE TABLE"` 时看到了备份文件的真面目——**它就是 SQL 语句的集合**：

```sql
CREATE TABLE `employees` (
  `id` int NOT NULL,
  `name` varchar(50) DEFAULT NULL,
  ...
INSERT INTO `employees` VALUES (1,'zhangsan','ops'),...
```

所以：
- **备份** = 把数据库当前状态导出成一串 SQL
- **恢复** = 让 MySQL 从头执行这串 SQL（建库→建表→插数据）
- 备份文件可以 `cat`、`grep`、甚至手动编辑——它就是普通文本

### ② mysqldump 的两种姿势（今天最容易混的点）

| 命令 | 备份文件里含什么 | 恢复时 |
|---|---|---|
| `mysqldump ... test > f.sql` | 只有表结构和数据 | **必须先自己 `CREATE DATABASE test`** 再灌入 |
| `mysqldump ... --databases test > f.sql` | 多了 `CREATE DATABASE` + `USE test` | **一条命令搞定**（今天用的） |
| `mysqldump ... --all-databases > f.sql` | 所有库（含 mysql 系统库、用户、权限） | 恢复连账号权限一起重放（今天的 1.3M 就是它） |

**判断依据**：打开备份文件看开头，有没有 `CREATE DATABASE` 语句。

### ③ 备份策略三要素（生产视角，面试必说）

1. **定时自动化**：cron 每天 2 点跑 mysqldump，人不能靠记忆
2. **异地存储**：备份和数据库放同一台机器，机器炸了备份一起没（今天备份在 `~/dbbackup/` 只是教学，生产要传走）
3. **定期演练恢复**：就是我们今天做的事——**没验证过恢复的备份等于没有备份**

进阶概念（面试加分）：**RPO**（能容忍丢多少数据）/ **RTO**（能容忍停多久）。mysqldump 是逻辑备份，数据量大时用物理备份工具（XtraBackup）或主从复制。

### ④ 为什么备份账号不用 root

今天建的 `bk` 账号，只用它做备份/恢复，root 密码不给备份脚本碰——**最小权限原则**：出了事（脚本被注入、密码泄露）损失范围可控。

> 说句实话：手册里 `GRANT ALL ON *.*` 给了 bk 全部权限，其实是简化版。生产上更规范的是只给 `SELECT, LOCK TABLES, SHOW VIEW, EVENT, TRIGGER` + 恢复时另授权。**知道"现在是简化、生产要收紧"本身就是加分项。**

### ⑤ Redis 为什么快（面试必问）

三件事凑出来的：

1. **数据在内存**：读写不碰磁盘，纳秒~微秒级
2. **单线程命令执行**：没有锁竞争、没有上下文切换，命令排队执行
3. **I/O 多路复用（epoll）**：一个线程同时盯着成千上万个网络连接，谁有数据就处理谁

> 单线程不是劣势——Redis 的瓶颈在内存和网络带宽，不在 CPU。这也是为什么生产上**提升 Redis 性能靠加内存/加机器（集群），而不是换多线程**。

### ⑥ 从 0 开始理解五种数据类型（结合今天的每一条命令）

#### 第 0 步：Redis 的世界观——一面储物柜墙

在讲五种类型之前，先把 Redis 的运行模型想明白。把 Redis 想象成**一面巨大的储物柜墙**：

- 每个柜子有一个**唯一的名字**（叫 **key**），比如 `user:1`
- 每个柜子里放**一样东西**（叫 **value**）
- 存东西 = 告诉 Redis "把这个值放进叫 xxx 的柜子"
- 取东西 = 告诉 Redis "把 xxx 柜子里的东西给我"

**所有 Redis 命令都是围绕"key → value"这一对展开的。** 而 value 的"形状"有五种——这就是五种数据类型。**选哪种，取决于你要存的东西长什么样**，就像选容器：装水用杯、装米用袋、装书用箱。

**和 MySQL 的根本区别**（为什么两个都要有）：

| | MySQL | Redis |
|---|---|---|
| 数据在哪 | 磁盘（持久，掉电不丢） | 内存（飞快，掉电靠持久化机制抢救） |
| 怎么找数据 | 写 SQL（SELECT ... WHERE） | 直接报 key 名（GET user:1） |
| 速度量级 | 毫秒级 | 微秒级（快 1000 倍） |
| 角色 | **仓库**：数据不能丢 | **手边工具包**：要快但容量有限 |

#### redis-cli 的两种用法

今天你用的全是"**单命令模式**"——一条命令跑完就退出：

```bash
redis-cli set user:1 "jasper"
```

还有"**交互模式**"：

```bash
redis-cli              # 敲这个进入，提示符变成 127.0.0.1:6379>
set user:1 "jasper"    # 之后不用再打 redis-cli 前缀，直接敲命令
get user:1
exit                   # 退出
```

两种方式效果完全一样。连续操作用交互模式，脚本里用单命令模式。

#### key 的命名惯例（社区通用，不是语法强制）

今天你敲的 key 都长这样：`user:1`、`info:1`、`tags:1`。**冒号分层**是 Redis 社区惯例：

```
对象:ID          user:1       → 1 号用户
对象:ID:属性     user:1:age   → 1 号用户的年龄
业务:日期        rank:20260906 → 9 月 6 日的排行榜
```

好处：一眼看出这个 key 归谁管、是什么。生产环境 key 成千上万，没有命名规范就是灾难。

---

#### 类型 ① string——一格储物柜放一样东西

**生活类比**：一张便利贴，写着内容贴在墙上，撕下来就能看。

**你要解决的问题**："我就想记住一个值，之后能取出来。"——最朴素的需求，string 就是为此而生。

**你今天敲的命令，逐词拆解**：

```
redis-cli  set   user:1     "jasper"
   │      │       │            │
   │      │       │            └─ 值：要存的内容
   │      │       └────────────── key：柜子的名字
   │      └────────────────────── 动作：SET = 存
   └───────────────────────────── Redis 的命令行客户端工具
```

回复 `OK` = 存好了。

```
redis-cli  get   user:1
   │      │       │
   │      │       └────────────── 要取的 key
   │      └────────────────────── 动作：GET = 取
```

回复 `"jasper"` = 柜子里的东西。**注意值带引号**——这是 redis-cli 显示字符串的方式，说明类型是字符串。

**string 的隐藏技能**（面试常提）：
- `INCR counter` → 数字自动 +1（**原子操作**，并发下也不会数错，适合点赞数、库存）
- `SET lock:1 "holder" NX` → NX = 不存在才设置（**分布式锁**的核心）
- `SET session:abc "..." EX 3600` → EX = 3600 秒后自动删除（**会话缓存**）

**MySQL 等价物**：一张 (k, v) 两列表格中的一行。但 Redis 取一条是微秒级，MySQL 要走索引查询。

#### 类型 ② hash——一个柜子里面有多个小格子

**生活类比**：一个抽屉，里面分成几个小格子：名字格、年龄格。抽屉整体有个名字，每个小格子也有名字。

**你要解决的问题**：一个对象有**多个属性**（姓名、年龄）。用 string 存就要拆成 `user:1:name`、`user:1:age` 两个 key——零碎、难管理。hash 把一个对象的所有属性**归到一个 key 下面**。

**你今天敲的命令，逐词拆解**：

```
redis-cli  hset  info:1   name     jasper    age     25
   │      │      │         │         │       │       │
   │      │      │         │         │       │       └─ 字段 2 的值
   │      │      │         │         │       └───────── 字段 2 的名（小格子名）
   │      │      │         │         └───────────────── 字段 1 的值
   │      │      │         └─────────────────────────── 字段 1 的名
   │      │      └───────────────────────────────────── key（抽屉名）
   │      └──────────────────────────────────────────── 动作：HSET = 设置 hash 字段
   └─────────────────────────────────────────────────── 客户端
```

**一条命令可以同时设多个字段**（字段名、值成对出现）。回复 `(integer) 2` = **新建了 2 个字段**。

⚠️ 这个数字的细节：如果 name 已经存在、这次只是把 jasper 改成 jerry，**不计数**；只有新建字段才计数（Redis 7 行为）。

**读**——把整个抽屉的东西全拿出来：

```
redis-cli  hgetall  info:1
   │      │         │
   │      │         └──────── key
   │      └────────────────── 动作：HGETALL = 取所有字段和值
```

输出：
```
1) "name"     ← 字段 1 的名
2) "jasper"   ← 字段 1 的值
3) "age"      ← 字段 2 的名
4) "25"       ← 字段 2 的值
```

**关键读法：两个一行成一对**——1)-2) 是 name=jasper，3)-4) 是 age=25。这就是你当时觉得"和预期不一样"的地方：**Redis 的列表回复永远是一行一个元素、带 N) 编号**，而 hgetall 的元素顺序是"字段名、值"交替。

只取一个字段：

```
redis-cli hget info:1 name    → "jasper"
```

**MySQL 等价物**：一行记录。`info:1` 这行的 name 列、age 列。区别是 Redis 不用建表——字段想加就加。

#### 类型 ③ list——一串糖葫芦，两头都能插

**生活类比**：排队上车，可以从队头塞人，也可以从队尾塞人。

**你要解决的问题**：一组**有先后顺序**的东西。比如消息队列——"先到的任务先处理"；朋友圈——"最新的动态在最上面"。

**你今天敲的命令，逐词拆解**：

```
redis-cli  lpush  queue   job1  job2  job3
   │      │       │        │
   │      │       │        └─────────── 依次往左端塞 3 个值
   │      │       └──────────────────── key
   │      └──────────────────────────── 动作：LPUSH = 从左边（队头）塞
   └─────────────────────────────────── 客户端
```

回复 `(integer) 3` = 塞完之后列表总长度是 3。

**读**——取一段区间：

```
redis-cli  lrange  queue   0    -1
   │      │        │       │     │
   │      │        │       │     └─ 结束下标：-1 = 最后一个元素
   │      │        │       └─────── 开始下标：0 = 第一个元素
   │      │        └─────────────── key
   │      └──────────────────────── 动作：LRANGE = 取下标区间
   └─────────────────────────────── 客户端
```

**`0 -1` 是"从头取到尾"的固定写法**（Redis 下标从 0 开始，**-1 表示最后一个，-2 表示倒数第二个**）。

输出：
```
1) "job3"   ← 最后塞的在最前面！
2) "job2"
3) "job1"
```

**为什么是倒的**：lpush 是**从左边塞**——job1 先塞、被后来者一路往右挤；job3 最后塞、停在队头。所以"从队头开始读"看到的顺序是 job3→job2→job1。这不是 bug，是**后进先出**（LIFO）——消息队列恰恰要这个特性。

对应的取队尾操作：`rpop queue` → 从右边弹出一个（队头的先被处理掉，公平）。

**MySQL 等价物**：一张带时间戳的表 + `ORDER BY created_at DESC LIMIT N`。Redis 不用排序——插入时就维护好了顺序。

#### 类型 ④ set——一袋不重复的弹珠

**生活类比**：一盒弹珠，两个同色的放进去自动只剩一颗。

**你要解决的问题**：**去重**。统计"今天有多少个不同用户来过"——同一个用户来 100 次只能算 1 次。用 list 存会有 100 条重复，用 set 天然只剩 1 条。

**你今天敲的命令，逐词拆解**：

```
redis-cli  sadd  tags:1   ops  linux  monitoring
   │      │       │        │
   │      │       │        └── 依次塞 3 个成员（一次可塞多个）
   │      │       └─────────── key
   │      └─────────────────── 动作：SADD = set add（往集合加成员）
   └────────────────────────── 客户端
```

回复 `(integer) 3` = 新增了 3 个成员。**如果塞重复的**（再 `sadd tags:1 ops`），回复 `(integer) 0`——集合里已经有了，不加。

**读**：

```
redis-cli  smembers  tags:1
   │      │          │
   │      │          └──────── key
   │      └─────────────────── 动作：SMEMBERS = 取所有成员
```

输出 `monitoring, linux, ops`——**顺序是乱的**（set 无序，这次按字母排纯属巧合）。别指望 set 有顺序——要顺序用 list 或 zset。

**set 的杀手锏：集合运算**（MySQL 做这个要写 JOIN，Redis 一条命令）：

```
sadd a:tags sports music      # A 的爱好
sadd b:tags music travel      # B 的爱好
sinter a:tags b:tags          → music（SINTER = 交集：共同爱好）
```

场景：共同好友、共同关注、抽奖去重、UV 统计（`SCARD` 数成员个数）。

#### 类型 ⑤ zset——带分数的记分牌（Redis 最有特色的结构）

**生活类比**：比赛记分牌：每个选手挂一个分数，**牌子自动按分数从高到低排好**，你随时能念出前三名。

**你要解决的问题**：**排行榜**。MySQL 里要 `ORDER BY score DESC`（数据量大就慢），zset 在**插入时自动维护顺序**，取前 N 名永远是微秒级。

**你今天敲的命令，逐词拆解**：

```
redis-cli  zadd  rank   100   alice   90    bob   85   carol
   │      │      │      │      │      │     │     │    │
   │      │      │      │      │      │     │     │    └─ 成员 3
   │      │      │      │      │      │     └─────── 分数 3
   │      │      │      │      │      └───────────── 成员 2
   │      │      │      │      └──────────────────── 分数 2
   │      │      │      └─────────────────────────── 成员 1
   │      │      └────────────────────────────────── 分数 1
   │      └───────────────────────────────────────── key
   └──────────────────────────────────────────────── 动作：ZADD
```

**注意写法**：**分数在前、成员在后，一对一对交替**——这是 zadd 的语法规定，和 hset 的"字段 值"成对异曲同工。

**读**——排行榜要分数从高到低：

```
redis-cli  zrevrange  rank  0  -1  withscores
   │      │           │     │   │   │
   │      │           │     │   │   └─ 顺带把分数也返回
   │      │           │     │   └───── 到最后一名
   │      │           │     └───────── 从第 0 名开始
   │      │           └─────────────── key
   │      └─────────────────────────── rev = reverse（倒序：分数高→低）
   └────────────────────────────────── 客户端
```

输出：
```
1) "alice"   ← 第 1 名
2) "100"     ← 他的分数
3) "bob"     ← 第 2 名
4) "90"
5) "carol"   ← 第 3 名
6) "85"
```

又是**成对格式**（成员/分数交替）。去掉 `withscores` 就只返回成员名。`zrevrange` 的 rev 就是倒序——正序用 `zrange`（分数低→高）。

**底层是什么（面试加分）**：跳表（skiplist）——给有序链表加了"高速公路索引"的多层结构，查询平均 O(logN)，实现比平衡树简单、范围查询还特别顺手。

**什么时候用**：排行榜、热搜榜、**延迟队列**（分数存"该执行的时间戳"，后台定时取到期任务）。

#### 五种类型怎么选（一张表收尾）

| 类型 | 存什么 | 写 | 读 | 今天你敲的 | 典型场景 |
|---|---|---|---|---|---|
| string | 一个值 | `set k v` | `get k` | user:1 | 缓存、计数器、锁 |
| hash | 一个对象的多个字段 | `hset k f v` | `hgetall k` / `hget k f` | info:1 | 资料、购物车 |
| list | 有序可重复序列 | `lpush k v` | `lrange k 0 -1` | queue | 消息队列、最新列表 |
| set | 无序不重复 | `sadd k v` | `smembers k` / `sinter` | tags:1 | 去重、共同关注 |
| zset | 带分数自动排序 | `zadd k s m` | `zrevrange k 0 -1 withscores` | rank | 排行榜、延迟队列 |

### ⑦ redis-cli 的回复格式（今天的"格式错觉"）

所有列表型输出都是 `N)` 编号 + 每行一个元素：

```
1) "name"     ← 字段
2) "jasper"   ← 值        hgetall 是字段/值交替
3) "age"
4) "25"
```

**两个一行成一对**。`lrange`/`smembers`/`zrange` 全都这个格式。看到它不要慌，是标准输出。

### ⑧ 持久化：RDB vs AOF（今天的 config get 就是活证据）

```
config get save         → "3600 1 300 100 60 10000"   ← RDB 规则
config get appendonly   → "no"                         ← AOF 默认关
```

**RDB 规则解读**：`3600 1` = 1 小时内至少 1 次修改就快照；`300 100` = 5 分钟内 100 次；`60 10000` = 1 分钟内 1 万次。

| | RDB（快照） | AOF（日志） |
|---|---|---|
| 本质 | 定时把内存全量 dump 成二进制 | 每条写命令追加到文件 |
| 文件体积 | 小 | 大 |
| 恢复速度 | 快（直接载入） | 慢（重放命令） |
| 丢数据 | 多（两次快照之间全丢） | 少（appendfsync everysec 最多丢 1 秒） |
| 默认 | ✅ 开 | ❌ 关 |

**Redis 4.0+ 支持混合持久化**（aof-use-rdb-preamble）：RDB 做头、AOF 做增量，兼顾恢复速度和数据安全，是生产推荐。

### ⑨ 大 key：`--bigkeys` 与 UNLINK

**大 key 的危害**：单个 key 的 value 特别大（比如一个 100 万元素的 list），读它占带宽、删它卡主线程、还会造成集群内存倾斜。

**`--bigkeys` 的聪明之处**：它用 **SCAN** 渐进扫描（不会像 `KEYS *` 那样把 Redis 卡死），统计每种类型的最大 key。今天输出里五种类型的最大 key 全列出来了。

**删大 key 用 `UNLINK` 不用 `DEL`**：
- `DEL`：同步删除，key 越大阻塞越久，**期间所有请求排队**
- `UNLINK`：主线程只把 key 摘掉，真正的内存释放在后台线程做

### ⑩ 缓存三大经典问题（背这张表，面试分水岭）

| 名词 | 是什么 | 一句话记忆 | 对策 |
|---|---|---|---|
| **穿透** | 查**根本不存在**的数据，缓存永远没有，每次砸到 DB | "查不存在的" | 缓存空值（短 TTL）、布隆过滤器 |
| **击穿** | 一个**热点 key 恰好过期**，瞬时大量请求砸到 DB | "一个热点过期" | 热点永不过期、互斥锁重建 |
| **雪崩** | **大量 key 同时过期**（或 Redis 整个挂了） | "集体过期" | 过期时间加随机抖动、集群高可用 |

区分诀窍：**穿透是"查没有的数据"，击穿是"一个热点的锅"，雪崩是"一大片的锅"**。

---

## 2. 完整步骤回顾

### 2.0 前置：sv01 磁盘扩容实战（20G → 60G）

**扩容决策（正确的三段式）**：
1. 算账：D19 要装 MySQL(~2G) + Redis，未来 D25.5 要 Docker(~600M) + kind 镜像(~1.5G)，现有 3.4G 必然不够 → **扩容是必然**
2. 快照处理：旧快照与当前状态一致（验证过的可用环境），**删旧快照 → 扩容 → 拍新快照**，既解开"有快照不能扩容"的限制，又让虚拟机回到满速
3. 容量：直接 60G，宿主机磁盘吃得住，一次到位

**重要发现：VG 里白捡 8.22G**

```
sda = 60G（VMware 扩展生效）
sda3 = 18.2G（原盘就是 20G！）
LV = 10G，vgs 显示 VFree = 8.22g   ← Ubuntu 安装器默认只把一半给根分区
```

> ⚠️ **"磁盘满"排查的隐藏一步**：`df -h` 报满之后，先 `sudo vgs` 看 VG 有没有空闲。**很多时候空间就在 VG 里躺着，一条 `lvextend` 白捡 8G**，根本不用加硬盘。

**扩容四步（一次成功）**：

```bash
sudo growpart /dev/sda 3                                     # 分区 18.2G → 58.2G
sudo pvresize /dev/sda3                                      # PV 认下新空间
sudo lvextend -l +100%FREE /dev/mapper/ubuntu--vg-ubuntu--lv # LV 10G → 58.22G
sudo resize2fs /dev/mapper/ubuntu--vg-ubuntu--lv             # 文件系统在线扩（不停机）
```

**四步的层次比喻（少任何一步都不行）**：

| 步骤 | 命令 | 比喻 |
|---|---|---|
| 确认 | `lsblk` | 看仓库**地皮**是不是变大了 |
| 1 | `growpart` | 把**围墙**（分区）往外挪 |
| 2 | `pvresize` | 告诉 LVM"你的地变大了" |
| 3 | `lvextend` | 把新地皮**划给**根分区房间 |
| 4 | `resize2fs` | 让文件系统**真正能用**这些空间 |

地皮大了围墙没挪=用不上；围墙挪了房间没扩=还是小；房间扩了文件系统没刷新=显示旧大小。

**验证**：`df -h /` → `58G 5.9G 49G 11%`；`vgs` → `VFree=0`；`zabbix-agent2` → active

### 2.1 MySQL 备份恢复七阶段

| 阶段 | 关键命令 | 结果 |
|---|---|---|
| A 安装 | `apt install mysql-server` + `enable` | 8.0.46 active |
| B 造数据 | `CREATE DATABASE test` + employees 表 3 行 | SELECT 可见 |
| C 备份账号 | `CREATE USER 'bk'@'localhost' IDENTIFIED BY 'Backup123!'` + `GRANT ALL ON *.*` | `mysql -u bk -p'Backup123!'` 登录成功 |
| D 备份 | `--all-databases`（1.3M）+ `--databases test`（2.2K） | grep 可见 CREATE TABLE |
| E 删库 | `DROP DATABASE test` | SHOW DATABASES 无 test |
| F 恢复 | `mysql -u bk -p'...' < test_20260906.sql` | 无报错（不用先建库，dump 自带） |
| G 验收 | `SELECT * FROM test.employees` | **3 行原样回来** ✅ |

### 2.2 Redis 五阶段

| 阶段 | 关键命令 | 结果 |
|---|---|---|
| R1 安装 | `apt install redis-server` + `enable --now` | active |
| R2 连通 | `redis-cli ping` | PONG |
| R3 五类型 | set/hset/lpush/sadd/zadd + 对应读命令 | 全部符合预期 |
| R4 大 key | `redis-cli --bigkeys` | 五种类型最大 key 全列出 |
| R5 持久化 | `config get save` / `appendonly` | RDB 开（三条规则）/ AOF 关 |

---

## 3. 排障与观察实录

### 观察一：`SELECT * FROME` → ERROR 1064（手误的教学价值）

```
ERROR 1064 (42000): You have an error in your SQL syntax; ... near 'FROME test.employees'
```

**判读方法**：`1064 = 语法错误`——**SQL 关键字拼错了**（FROME ≠ FROM），和数据库状态、数据是否存在**毫无关系**。改对关键字立刻通过。

> 见到报错先分类：**1064 查拼写、1045 查账号密码、1146 查表是否存在、1049 查库是否存在**。错误码定位比通读报错文本快得多。

### 观察二：hgetall 输出的"格式错觉"

`hgetall info:1` 返回 4 行而不是"两行键值对"，让人怀疑输出不对。实际是 **field/value 交替**（1)-2) 一对，3)-4) 一对），是 redis-cli 所有列表型回复的标准格式。**读 Redis 输出先认格式，再判对错。**

### 观察三：`-p` 空格坑零复发

D16 第一次踩、今天全程 `-p'Backup123!'` 紧贴零失误——**三次踩坑 + 复盘，变成了肌肉记忆**。教训的价值不在"记住"，在"形成流程"。

### 观察四：`[Warning] Using a password on the command line interface can be insecure`

不影响功能，但要知道它为什么出现：**命令行里的密码会留在 shell history（`~/.bash_history`）里**。生产的正规做法：
- `mysql_config_editor set --login-path=bk --user=bk --password`（加密存凭据，之后 `mysql --login-path=bk`）
- 或 `~/.my.cnf` 配置文件（chmod 600）

---

## 4. 知识结晶：七颗钉子

1. **备份文件 = 一坨 SQL，恢复 = 重放**。能 cat、能 grep、能手动改——备份不再是黑盒。
2. **没验证过恢复的备份等于没有备份**。备份策略三要素：定时自动化、异地存储、定期演练。
3. **快照防手滑，备份防灾难**。快照是增量 delta（依赖原始盘、会膨胀、拖慢 I/O），验证通过立刻删；备份是独立完整副本。
4. **"磁盘满"排查两层走**：先 `df -h` 看文件系统，再 `sudo vgs` 看 VG 空闲——**空间可能就在 VG 里躺着**。LVM 四步在线扩容（growpart→pvresize→lvextend→resize2fs）不停机，这就是 LVM 存在的意义。
5. **Redis 快 = 内存 + 单线程无锁 + epoll**。瓶颈在内存和网络，不在 CPU，所以扩性能靠加内存/加机器。
6. **缓存三兄弟**：穿透查"不存在的"、击穿是"一个热点过期"、雪崩是"一大片过期/Redis 挂"。对策各不同，背表。
7. **删大 key 用 UNLINK 不用 DEL**：异步不阻塞。发现用 `--bigkeys`（SCAN 渐进，不卡 Redis）。

---

## 5. 面试话术

### MySQL 备份恢复（1 分钟版）

> "我做过完整的 MySQL 备份恢复演练。环境是 Ubuntu 上的 MySQL 8.0，我先建了备份专用账号 bk，遵循最小权限——备份不用 root，就算脚本被注入损失也可控。然后用 mysqldump 做了两种备份：全量 --all-databases 和单库 --databases。接着故意 DROP 掉测试库，再用备份文件恢复，数据完整回来了。
>
> 这个过程中我理解了备份文件的本质：它就是一坨按顺序执行的 SQL——建库、建表、插数据，恢复就是把这套 SQL 重放一遍。区别只在 mysqldump 的参数：不带 --databases 就不含建库语句，恢复前要自己建库；带了就一条命令搞定。
>
> 生产环境我还会补三样：cron 定时自动备份加过期清理、备份文件传到异地（防整机故障）、以及定期把备份恢复到测试机验证可用性——因为没验证过恢复的备份等于没有备份。"

### Redis（2 分钟版）

> "Redis 是内存数据库，快的原因有三点：数据在内存、单线程命令执行没有锁竞争、epoll I/O 多路复用让它一个线程能扛几万连接。
>
> 我部署后把五种数据类型都实操了一遍：string 做缓存和计数器；hash 存对象；list 做消息队列——lpush 从左端塞，lrange 出来是倒序的；set 做去重和交集运算；zset 做排行榜，底层是跳表。
>
> 持久化方面我确认了默认配置：RDB 开着，规则是 1 小时 1 次、5 分钟 100 次、1 分钟 1 万次这三个触发条件；AOF 默认关。生产上我会开混合持久化——RDB 做全量头、AOF 做增量，恢复快又不丢数据。
>
> 运维层面我掌握两个实用命令：--bigkeys 用 SCAN 渐进扫描找大 key，不阻塞业务；删大 key 用 UNLINK 不用 DEL——DEL 是同步的，删一个百万元素的 key 会卡住 Redis 几秒，所有请求全堵。
>
> 缓存的三个经典问题我能区分：穿透是查根本不存在的数据，对策是缓存空值加布隆过滤器；击穿是一个热点 key 恰好过期，对策是热点不过期加互斥锁；雪崩是大量 key 同时过期或者 Redis 整体挂掉，对策是过期时间加随机、集群高可用。"

### 高频追问 10 条

**Q1：mysqldump 和 XtraBackup 的区别？**
A：mysqldump 是逻辑备份（导出 SQL），可读、可跨版本、跨存储引擎，但大库慢（几百 G 要几小时）；XtraBackup 是物理备份（拷贝数据文件），快、支持热备不锁表，但只能在同版本 MySQL 恢复。小库用 dump，大库用 XtraBackup。

**Q2：全量备份怎么配合增量？**
A：MySQL 开 binlog 后：每天凌晨全量 dump + 实时 binlog。恢复时先放全量，再重放到指定时间点的 binlog（`mysqlbinlog --start-datetime ... | mysql`）。这就是"恢复到任意一秒"的能力——误删数据后最优解。

**Q3：Redis 为什么单线程还这么快？**
A：数据在内存（纳秒级）、无锁（单线程串行执行命令）、epoll 多路复用（一个线程管几万连接）。瓶颈在内存和网络带宽，不在 CPU。

**Q4：缓存和数据库的一致性怎么保证？**
A：cache-aside 模式：读时先查缓存、未命中查 DB 并回填；写时**先更新 DB、再删缓存**（不是更新缓存）。要更强一致性可用延迟双删或订阅 binlog（Canal）异步删缓存。

**Q5：Redis 挂了怎么办？**
A：①持久化文件恢复（RDB/AOF）；②生产配主从 + 哨兵（Sentinel）自动故障转移，或 Cluster 分片集群；③应用侧要有限流降级兜底——缓存挂了不能把数据库也打死。

**Q6：什么数据不适合放 Redis？**
A：写多读少的冷数据（缓存意义不大）、超大 value（大 key 风险）、强一致要求且不允许丢失的核心数据（内存数据库掉电风险，AOF 也只保 1 秒）。

**Q7：bigkeys 找到之后怎么处理？**
A：看业务能不能拆——大 list 拆成多个小 key（分片）；能整删的用 UNLINK 异步删；4.0+ 还可以用 lazyfree 相关配置让过期删除也异步。

**Q8：Redis 的淘汰策略了解吗？**
A：内存到 maxmemory 上限时按 maxmemory-policy 淘汰：noeviction（默认，拒绝写）、allkeys-lru（最常用）、volatile-lru（只淘汰带过期时间的）、allkeys-random 等 8 种。缓存场景一般配 allkeys-lru。

**Q9：过期 key 是怎么删的？**
A：惰性删除（访问时发现过期才删）+ 定期删除（后台随机抽检过期 key）。所以过期 key 不会立刻消失，但有这两层兜底不会堆积成灾。

**Q10：主从、哨兵、集群怎么选？**
A：主从复制：数据冗余+读写分离，但主挂了要人工切换；哨兵：在主从基础上加自动故障转移（推荐中小规模）；Cluster：数据分片到多节点，解决单机内存上限和写瓶颈（大规模）。

---

## 6. 命令速查表

### 磁盘扩容（LVM 四步）

| 用途 | 命令 |
|---|---|
| 看分区结构 | `lsblk /dev/sda` |
| **看 VG 空闲（磁盘满第二查）** | `sudo vgs` |
| 扩分区 | `sudo growpart /dev/sda 3` |
| PV 认新空间 | `sudo pvresize /dev/sda3` |
| LV 全量扩展 | `sudo lvextend -l +100%FREE /dev/mapper/ubuntu--vg-ubuntu--lv` |
| 文件系统在线扩 | `sudo resize2fs /dev/mapper/ubuntu--vg-ubuntu--lv` |
| 新加盘时 | `pvcreate /dev/sdb` → `vgextend ubuntu-vg /dev/sdb` → lvextend → resize2fs |

### MySQL 备份恢复

| 用途 | 命令 |
|---|---|
| 免密进（Ubuntu root） | `sudo mysql` |
| 专用账号登录 | `mysql -u bk -p'Backup123!'`（**-p 紧贴**） |
| 全量备份 | `mysqldump -u bk -p'...' --all-databases > all_$(date +%Y%m%d).sql` |
| 单库备份（含建库） | `mysqldump -u bk -p'...' --databases test > test_$(date +%Y%m%d).sql` |
| 恢复 | `mysql -u bk -p'...' < 文件名.sql` |
| 验证 | `mysql -u bk -p'...' -e "SELECT * FROM test.employees;"` |
| 看备份内容 | `grep -A2 "CREATE TABLE" xxx.sql` |

### Redis

| 用途 | 命令 |
|---|---|
| 连通测试 | `redis-cli ping` → PONG |
| string | `set` / `get` |
| hash | `hset` / `hgetall` |
| list | `lpush` / `lrange key 0 -1` |
| set | `sadd` / `smembers` |
| zset | `zadd` / `zrevrange key 0 -1 withscores` |
| 大 key 扫描 | `redis-cli --bigkeys` |
| 持久化配置 | `redis-cli config get save` / `config get appendonly` |
| 异步删大 key | `UNLINK key`（不用 DEL） |

---

## 7. 遗留与下一步

### D19 可选补强（不阻塞进度）

- [ ] **备份自动化**：cron 每天 2 点 mysqldump + 保留 7 天（`find -mtime +7 -delete`）
- [ ] **Redis 设密码**：`requirepass` 配置（当前裸奔，内网可接受但要知道）
- [ ] **备份异地**：scp 到 sv02 或对象存储
- [ ] 把 MySQL/Redis 挂进 Zabbix 监控（模板 `MySQL by Zabbix agent` 需配监控账号）

### 后续日程

- **D20-D21**：缓冲 + 掘金文章《Zabbix 从装到告警触发，我踩了这些坑》+ 背场景题 6 题
  - 素材已攒够 10+ 坑：sed 三次静默失败、MySQL `-p` 空格、nginx 8080 vs 80、Templates 弹窗、ufw 10050、No message defined 两层宏、nohup Exit 125、msi 装错机器、inode 告警、1064 手误判读
- **D22-D25**：云上最小上线（阿里云轻量 2C2G，已注册实名）
- **D25.5**：kind 单节点 K8s（磁盘已扩到 60G，空间无忧）
- **项目 5**：eNSP 华为交换机（石家庄 6-8K 岗最对口，建议做）

---

## 附：时间线

| 时间 | 事件 |
|---|---|
| 09:40 | D19 开工，df -h 发现 3.4G 可用，决定扩容 |
| 09:44 | 用户判断"旧快照无用，删了再扩"，获确认 |
| 09:48 | VMware 磁盘扩展到 60G |
| 09:53 | lsblk 确认 sda=60G，发现原盘 20G / VG 白躺 8.22G |
| 09:55 | LVM 四步完成，df 49G 可用，agent 无恙 |
| 09:58 | 拍基线快照 `D19前-基线-60G` |
| 10:06 | MySQL 8.0.46 装好 |
| 10:09 | 备份完成（1.3M + 2.2K） |
| 10:14 | 删库 → 恢复 → 数据回来（板块一通关） |
| 10:19 | Redis 部署 + PONG |
| 10:24 | 五类型实操全通 |
| 10:27 | --bigkeys + 持久化确认（D19 通关） |
