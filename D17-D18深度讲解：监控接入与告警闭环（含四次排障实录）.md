# D17-D18 深度讲解：监控接入与告警闭环（含四次排障实录）

> 日期：2026-09-05（21:55 – 23:35）｜环境：VMware NAT `192.168.207.0/24`
> 参与机器：
> - **sv02**（Zabbix Server，`192.168.207.30`）—— 已在 D15-D16 装好 Zabbix 7.0.30
> - **sv01**（被监控端，`192.168.207.128`）—— 本日新装 zabbix-agent2
> 版本：Zabbix **7.0.30** / Ubuntu 24.04.4 LTS（noble）
> 状态：**sv01 接入监控，告警闭环完整打通，通知携带完整告警内容送达脚本**

---

## 开篇：这两天做的事，和 D15-D16 是什么关系

D15-D16 我们**造了一台监控服务器**（sv02 跑 Zabbix Server）。但一台只监控自己的 Zabbix，等于买了个消防系统只装在消防局里——**没意义**。

D17-D18 做的是**让这套系统真正开始干活**：

| | D15-D16 | D17-D18 |
|---|---|---|
| 做了什么 | 建监控中心 | 把设备接入 + 让告警真的响 |
| 打比方 | 盖消防局、装中控主机 | 在大楼里装烟雾传感器、接报警铃、做消防演练 |
| 成果 | `Zabbix server is running: Yes` | sv01 挂掉 → 3 分钟后告警带着完整信息送达 |

**本日最核心的一句话**：监控的价值不在于"能看"，而在于**"出事时它会告诉你"**。D18 的告警闭环，是整个 Zabbix 项目的**灵魂**——前面所有工作都是为了这一刻。

---

## 0. 这一天的产出总览

| # | 产出 | 验证方式 |
|---|---|---|
| 1 | sv01 安装并配置 `zabbix-agent2` | `systemctl is-active` = `active` |
| 2 | Web 创建主机 sv01，挂 `Linux by Zabbix agent` 模板 | Hosts 列表出现 sv01 |
| 3 | 端到端连通性验证 | `zabbix_get -k agent.hostname` 返回 `sv01` |
| 4 | **定位并修复防火墙故障** | `nc` 从 timed out → succeeded |
| 5 | sv02 安装 `zabbix-get` 排障工具 | 四连排查法可用 |
| 6 | 配置 Script 告警媒介 | Media types 里 `local-script` Enabled |
| 7 | **告警闭环打通** | Action log `Sent` + 脚本日志带完整宏内容 |
| 8 | 掌握告警四要素模型与状态机原理 | 见第 1 章 |
| 9 | 掌握 `refused` vs `timeout` 判别法 | 见排障二 |
| 10 | 掌握 Zabbix 两层宏体系 | 见排障四 |

---

## 1. 概念地基：告警到底是怎么跑通的

这是本日**最重要的理论部分**。搞懂这张图，Zabbix 的所有配置界面你都不会迷路。

### ① 告警四要素（缺一不可）

```
   ① Trigger        ② Action         ③ Media type      ④ User Media
   "什么时候算      "出问题了        "通过什么渠道      "发给谁"
    出问题？"        干什么？"         发出去？"
        │              │                 │                 │
        │              │                 │                 │
   磁盘 < 15%  ──►  匹配到动作  ──►  用 Script 发  ──►  发给 Admin
   agent 失联        "发通知给        执行 alert.sh        （绑定了
   CPU > 80%         Zabbix                                local-script）
                     administrators"
```

**四个环节任何一个断了，告警都发不出去。** 今天的排障四就是卡在 ② 和 ③ 的接口上。

| 要素 | 在 Web 哪里配 | 今天的取值 |
|---|---|---|
| ① Trigger | 模板自带 / `Data collection → Hosts → 主机 → Triggers` | `Linux: Zabbix agent is not available (for 3m)` |
| ② Action | `Alerts → Actions → Trigger actions` | `Report problems to Zabbix administrators` |
| ③ Media type | `Alerts → Media types` | `local-script`（Script 类型，调 `alert.sh`） |
| ④ User Media | `Users → Users → Admin → Media` | `local-script` / sendto `local` / 全天 / A H D |

### ② 告警状态机（为什么"时间没变"是对的）

```
        ┌──────────── 检测到问题 ────────────┐
        ▼                                     │
      OK ──────────────────────────────►  PROBLEM
       ▲                                     │
       │                                     │ 持续期间
       │                                     │ **不再重复发**
       └──────── 恢复（Recovery）────────────┘
```

**关键设计：同一个问题在恢复之前只报一次。**

这不是 bug，是**告警去重**——否则一台机器宕机 8 小时，你的手机会被同一条告警轰炸几百次，直接导致"告警疲劳"（alert fatigue），真出大事时反而没人看了。生产环境里这是**必备设计**。

> **今晚的困惑点**：用户第二次停 agent 后发现 Problems 页面的时间"没变"，以为出故障了。实际上 `Age` 列显示 `24m 22s` —— 铁证：这是**持续中的同一事件**，从 22:34 开始就没恢复过。
>
> **推论**：验证告警必须走完整循环 **"触发 → 恢复 → 再触发"**。只做半程（停了不停）永远看不到第二次通知。

### ③ 两层宏体系（今天最有技术含量的发现）

Zabbix 里有两组容易混淆的宏，它们作用在**不同的层**：

| 层 | 宏 | 作用 | 在哪配 |
|---|---|---|---|
| **消息内容层** | `{HOST.NAME}` `{EVENT.SEVERITY}` `{ITEM.LASTVALUE}` `{EVENT.DATE}` | 决定**消息长什么样** | Action 的 Custom message |
| **媒介参数层** | `{ALERT.SENDTO}` `{ALERT.SUBJECT}` `{ALERT.MESSAGE}` | 决定**传什么给脚本** | Media type 的 Script parameters |

**两层都配，告警内容才能完整传到脚本。** 今天只配了第一层，结果脚本拿到的 `$1 $2 $3` 全是空字符串。

**常用宏速查：**

| 宏 | 含义 | 今晚实际渲染成 |
|---|---|---|
| `{HOST.NAME}` | 主机名 | `sv01` |
| `{EVENT.SEVERITY}` | 严重等级 | `Average` |
| `{TRIGGER.STATUS}` | 触发器状态 | `PROBLEM` |
| `{EVENT.DATE} {EVENT.TIME}` | 发生时间 | `2026.09.05 23:33:45` |
| `{EVENT.NAME}` | 事件名 | `Linux: Zabbix agent is not available (for 3m)` |
| `{TRIGGER.DESCRIPTION}` | 触发器描述 | `For passive agents only, host availability is used with '3m' as a time threshold.` |
| `{ITEM.NAME}` | 监控项名 | `Zabbix agent availability` |
| `{ITEM.LASTVALUE}` | 当前值 | `not available (0)` |
| `{EVENT.AGE}` | 已持续时长 | `24m 22s` |
| `{EVENT.RECOVERY.DATE}` | 恢复时间 | （恢复模板用） |

### ④ 触发器为什么要带"3 分钟"窗口

模板里的触发器叫 `Zabbix agent is not available (for 3m)`，不是"不可用了就报"，而是"**连续 3 分钟**不可用才报"。

**为什么？防抖（flap protection）。** 网络抖一下、机器重启那一瞬间、交换机闪断——这些都会让 agent 短暂失联。如果不设时间窗口，你会在半夜被假告警吵醒。

**设计权衡**：3 分钟是"及时性"和"准确性"的平衡点。核心交易系统可能缩到 30 秒，边缘业务可能放宽到 10 分钟。**面试问"如何减少误报"，这就是第一个答案。**

---

## 2. 完整步骤回顾

### 阶段 0：sv01 加 Zabbix 仓库

```bash
wget https://mirrors.aliyun.com/zabbix/zabbix/7.0/ubuntu/pool/main/z/zabbix-release/zabbix-release_latest_7.0+ubuntu24.04_all.deb
sudo dpkg -i zabbix-release_latest_7.0+ubuntu24.04_all.deb
sudo sed -i 's#https://repo.zabbix.com#https://mirrors.aliyun.com/zabbix#g' /etc/apt/sources.list.d/zabbix.sources
sudo sed -i 's#https://repo.zabbix.com#https://mirrors.aliyun.com/zabbix#g' /etc/apt/sources.list.d/zabbix-tools.sources
sudo apt update
```

⚠️ 每个 sed 后面都要 `grep URIs` 回看（sed 静默失败，见排障三）。

### 阶段 A：装 agent2

```bash
sudo apt install -y zabbix-agent2
```

### 阶段 B：改配置（三行核心）

```bash
sudo sed -i 's/^Server=127.0.0.1/Server=192.168.207.30/' /etc/zabbix/zabbix_agent2.conf
sudo sed -i 's/^ServerActive=127.0.0.1/ServerActive=192.168.207.30/' /etc/zabbix/zabbix_agent2.conf
sudo sed -i 's/^Hostname=Zabbix server/Hostname=sv01/' /etc/zabbix/zabbix_agent2.conf
```

**必须回看**（三行缺一不可）：

```bash
grep -E '^Server=|^ServerActive=|^Hostname=' /etc/zabbix/zabbix_agent2.conf
```

| 配置项 | 含义 | 今晚取值 |
|---|---|---|
| `Server=` | **被动模式白名单**：允许谁来问我 | `192.168.207.30`（sv02） |
| `ServerActive=` | **主动模式目标**：我主动送给谁 | `192.168.207.30` |
| `Hostname=` | 我的身份标识 | `sv01` ← **必须和 Web 里填的一字不差** |

### 阶段 C：启动 + 开机自启

```bash
sudo systemctl restart zabbix-agent2
sudo systemctl enable zabbix-agent2
sudo ss -tlnp | grep 10050
```

预期：`LISTEN 0 4096 *:10050 *:* users:(("zabbix_agent2",...))`

⚠️ 注意看监听地址：如果是 `127.0.0.1:10050` 说明只监听本机，外地连不进来，要改 `ListenIP=0.0.0.0`。今晚是 `*:10050`（所有网卡），正常。

### 阶段 D：Web 创建主机

`Data collection → Hosts → Create host`

| 字段 | 填什么 | 为什么 |
|---|---|---|
| Host name | `sv01` | **必须和 agent 的 `Hostname=` 完全一致** |
| Visible name | `sv01` | 仅显示用 |
| Templates | **`Linux by Zabbix agent`**（只挂这一个） | 被动模式用这个，不要选 `...active` 版 |
| Host groups | `Linux servers` | 分组管理 |
| Interfaces → Agent | `192.168.207.128` / `10050` | sv01 的 IP + agent 默认端口 |
| Monitored by | `Server` | 不用 proxy |
| Enabled | ✅ | |

> **坑**：Templates 的 Select 弹窗里，"Template group" 点进去会列出所有模板组（Applications / Cloud / Databases / Network devices / **Operating systems** / Power / SAN / Server hardware / Telephony / Video surveillance）。Linux 模板在 `Templates/Operating systems` 下。第一次操作时容易把整个组全勾上（AIX/FreeBSD/HP-UX/macOS/Solaris/Windows 全来了），已纠正为**只留 `Linux by Zabbix agent`**。

### 阶段 E：装 zabbix-get 并做四连验证（在 sv02 上）

```bash
sudo apt install -y zabbix-get
```

**zabbix-get 四连排查法**（"Zabbix 不出数据"的标准排查顺序）：

| 测什么 | 命令 | 通过说明什么 |
|---|---|---|
| ① 网络层 | `nc -zv 192.168.207.128 10050 -w 5` | 端口可达、防火墙放行 |
| ② agent 活着 | `zabbix_get -s <IP> -k agent.ping` | agent 进程正常响应 |
| ③ **身份对不对** | `zabbix_get -s <IP> -k agent.hostname` | **Hostname 一致性**（最容易错的一环） |
| ④ 数据能不能取 | `zabbix_get -s <IP> -k system.uptime` | 监控项真能采到值 |

**今晚实测结果：**

```
agent.hostname → sv01        ✅ 身份一致
system.uptime  → 10943       ✅ 能采到真实数据
system.cpu.load → 0.000000   ✅ 监控项工作正常
```

### 阶段 F：配置告警媒介

**F1 — 建脚本（sv02 上）：**

```bash
sudo mkdir -p /usr/lib/zabbix/alertscripts
```

```bash
sudo tee /usr/lib/zabbix/alertscripts/alert.sh > /dev/null <<'EOF'
#!/bin/bash
echo "$(date '+%F %T') | ARGS[$#]: $@" >> /tmp/zabbix-alert.log
EOF
```

```bash
sudo chmod +x /usr/lib/zabbix/alertscripts/alert.sh
```

> 最终版脚本用 `$#` 和 `$@` 打印参数个数和内容——这是**诊断脚本**，能直接看清 Zabbix 到底传了几个参数。生产环境应改成 `echo "$1 | $2 | $3"` 或写结构化日志。

**为什么用 Script 而不是 Email？** 邮件要配 SMTP、要邮箱授权码、可能被网络限制——**在验证阶段引入这些外部依赖，出问题你分不清是 Zabbix 的锅还是邮件的锅**。Script 写文件 100% 可控，先证明"Zabbix 这条链路是通的"，再换真渠道。这个思路叫**先跑通再优化**（和 D16 先跑通 80 端口再谈改端口是同一个原则）。

**F2 — Web 建 Media type：**

`Alerts → Media types → Create media type`

| 字段 | 值 |
|---|---|
| Name | `local-script` |
| Type | **Script** |
| Script name | `alert.sh` |
| **Script parameters** | `{ALERT.SENDTO}` / `{ALERT.SUBJECT}` / `{ALERT.MESSAGE}` ← **三个都要加** |
| Status | Enabled |

**F3 — 给 Admin 绑媒介：**

`Users → Users → Admin → Media 标签 → Add`

| 字段 | 值 |
|---|---|
| Type | `local-script` |
| Send to | `local` |
| When active | `1-7,00:00-24:00` |
| Use if severity | 勾选 Average / High / Disaster |
| Enabled | ✅ |

⚠️ **加完必须回主页面点 Update 保存**——这是 Zabbix UI 最反直觉的地方，很多人只在弹窗里点 Add 就关了，一刷新配置全没。

**F4 — 配 Action 的消息模板：**

`Alerts → Actions → Trigger actions → Report problems to Zabbix administrators`

- **Operations** 标签 → 点那条 Operation 的 **Edit**
- 勾选 **`Custom message`**（Zabbix 7.0 默认不勾 → 不勾就是 `No message defined`）
- **Subject** 填：
  ```
  Problem: {EVENT.SEVERITY} on {HOST.NAME} - {EVENT.NAME}
  ```
- **Message** 填：
  ```
  Host:      {HOST.NAME}
  Severity:  {EVENT.SEVERITY}
  Status:    {TRIGGER.STATUS}
  Time:      {EVENT.DATE} {EVENT.TIME}
  Problem:   {EVENT.NAME}
  Details:   {TRIGGER.DESCRIPTION}
  Item:      {ITEM.NAME} = {ITEM.LASTVALUE}
  ```
- 点 Update → **回到 Operations 主页面再点一次 Update**

（Recovery operations 同理，可另配"已恢复"模板）

### 阶段 G：告警闭环验证（完整循环）

```bash
# sv01 上
sudo systemctl stop zabbix-agent2      # 制造故障
# 等 3 分钟 → Web Problems 出现红色告警
```

```bash
# sv01 上（先恢复，否则第二次停不会触发新告警）
sudo systemctl start zabbix-agent2
# 等 1-2 分钟 → Web 变绿，告警 RESOLVED
```

```bash
# sv01 上（再触发一次全新告警）
sudo systemctl stop zabbix-agent2
# 等 3 分钟
```

```bash
# sv02 上验收
cat /tmp/zabbix-alert.log
```

**最终验收结果：**

```
2026-09-05 22:51:32 | test-to | test-subject | test-body      ← 手动测试
2026-09-05 23:20:45 |  |  |                                   ← Script parameters 没配，参数为空
2026-09-05 23:33:45 | ARGS[3]: local Problem: Average on sv01 - Linux: Zabbix agent is not available (for 3m)
Host:      sv01
Severity:  Average
Status:    PROBLEM
Time:      2026.09.05 23:33:45
Problem:   Linux: Zabbix agent is not available (for 3m)
Details:   For passive agents only, host availability is used with '3m' as a time threshold.
Item:      Zabbix agent availability = not available (0)     ← ✅ 全链路打通
```

---

## 3. 排障实录（四个坑，全是真金白银）

### 排障一：Templates 弹窗搜不到 Linux——先证明数据在不在

**现象**：Web 创建主机时，Templates → Select → 搜索 `Linux` 无结果。

**错误思路**：怀疑安装不完整、模板没导入、要不要重装。

**正确做法——先用一条 SQL 定案：**

```bash
mysql -uzabbix -p'Zabbix@2026' -e "SELECT COUNT(*) FROM zabbix.hosts WHERE status=3;"
```

返回 **356** → 模板全在库里（`status=3` = 模板类型）。**问题在 UI 操作，不在安装。**

**真正原因**：Zabbix 7.0 的模板选择弹窗采用"延迟加载"设计——**默认不显示任何内容**，界面中央写着 "Filter is not set / Use the filter to display results"，必须先设筛选条件（选 Template group 或填 Name 后回车）才出列表。

**收获的方法论**：

> **遇到"找不到 X"，第一件事是证明 X 到底在不在，而不是纠结怎么点击。**
> 一条 SQL 就把"是不是装崩了要不要重装"这种大问题掐死在摇篮里。

**万能绕行路**（卡住就用）：**Templates 不是必填项。** 先填 Host name / Host groups / Agent 接口 → Add 保存 → 之后再进主机详情页的 Templates 标签页补挂，效果完全相同。**永远别让一个弹窗卡死整体进度。**

---

### 排障二：Availability 红色 + `timed out`——防火墙的锅

**现象**：sv01 红色，`nc -zv 192.168.207.128 10050 -w 5` 返回

```
nc: connect to 192.168.207.128 port 10050 (tcp) timed out: Operation now in progress
```

**三层定位法（逐层排除）：**

```bash
ping -c 3 192.168.207.128
# → 3 received, 0% packet loss ✅ 主机在线、三层可达
```

```bash
sudo ss -tlnp | grep 10050        # 在 sv01 上
# → LISTEN 0 4096 *:10050 *:* users:(("zabbix_agent2",pid=2314,fd=7)) ✅ 服务在监听所有网卡
```

```bash
hostname -I                        # 在 sv01 上
# → 192.168.207.128 ✅ IP 没变
```

```bash
sudo ufw status                    # 在 sv01 上
# → Status: active
#   OpenSSH   ALLOW  Anywhere
#   80/tcp    ALLOW  Anywhere      ← 只放了 22 和 80 ❌ 根因
```

**根因**：sv01（早期装的机器）的 ufw 是 active 且只放行 22/80，10050 被 ufw 默认策略 **DROP**。sv02（新装）一直是 inactive，所以之前在 sv02 上做实验从没暴露这个问题。

**修复：**

```bash
sudo ufw allow 10050/tcp
sudo ufw status | grep 10050       # 必须回看
nc -zv 192.168.207.128 10050 -w 5  # 回 sv02 复测
# → Connection to 192.168.207.128 10050 port [tcp/zabbix-agent] succeeded! ✅
```

**知识点（与 D16 那次凑成一对，面试必背）：**

| 现象 | 含义 | 人话 |
|---|---|---|
| `Connection refused` | 主机可达 + 端口无服务监听 + 防火墙放行 | **有人应门说"不在"** |
| `timed out` | 包石沉大海（防火墙 DROP / 路由不通） | **敲门没人理** |

补充：`ping` 通不代表端口通（ufw 默认不拦 ICMP）。

---

### 排障三：`sed` 静默失败——`#` 后面那个空格

**现象**：想把 `AlertScriptsPath` 的注释去掉，执行

```bash
sudo sed -i 's|^#AlertScriptsPath|AlertScriptsPath|' /etc/zabbix/zabbix_server.conf
```

结果 `grep` 回看，文件纹丝未动：

```
### Option: AlertScriptsPath
# AlertScriptsPath=/usr/lib/zabbix/alertscripts
```

**原因**：文件里是 `# AlertScriptsPath`（`#` 后**有空格**），命令匹配的是 `#AlertScriptsPath`（`#` 后**无空格**）—— 差一个空格，sed 匹配不上。

**这是今晚第三次踩 sed 静默失败**（前两次：Zabbix 源 http vs https、换源漏 `ubuntu/`）。三次全部通过 **grep 回看**抓到。

```bash
sudo sed -i 's|^# AlertScriptsPath|AlertScriptsPath|' /etc/zabbix/zabbix_server.conf   # 正确版
```

> **sed 铁律（第 N 次强调）**：匹配不到 → 不报错、不警告、退出码 0、什么都不做。
> **改配置文件：改之前备份 `.bak`，改之后必须 grep 回看。**

**顺带的发现**：其实这行不改也行。

```
### Option: AlertScriptsPath
# AlertScriptsPath=/usr/lib/zabbix/alertscripts
```

Zabbix 配置文件头部有说明：**"注释掉的内容 = 默认值"**。所以 `#` 状态恰恰说明当前用的就是 `/usr/lib/zabbix/alertscripts`，和我们的脚本目录完全一致。

> 💡 **Zabbix 配置文件的设计哲学**：把所有默认值写进注释，配置文件就能保持极简（只写你要改的那几行），同时随时可查"不改的话是什么"。**看到任何被 `#` 注释的 Zabbix 配置项，注释内容就是当前实际值。**（面试能讲）

**另一个附带的知识点**：

```bash
ls -l /etc/zabbix/zabbix_server.conf
# → -rw-r----- 1 root zabbix ...
```

权限是 **640**（root 可读写、zabbix 组可读、其他人不读），因为里面有明文数据库密码 `DBPassword=Zabbix@2026`。所以直接 `grep` 这个文件会 `Permission denied`，必须加 `sudo`。

> **配置文件里的明文密码 = 配置文件权限必须收紧**，这是运维基本功。和 `.ssh/authorized_keys` 要 600、`~/.ssh` 要 700 是同一个道理。

---

### 排障四：`No message defined for media type`——两层宏没打通

**这是今晚最有技术含量、也最值得记的坑。**

**现象链**：
1. 脚本手动执行成功：`sudo -u zabbix /usr/lib/zabbix/alertscripts/alert.sh "test-to" "test-subject" "test-body"` → 日志正常写入 ✅
2. Web 三查全过：Media type Enabled、Admin 绑定了 Media、Action Enabled ✅
3. 但告警触发后，`/tmp/zabbix-alert.log` 里**只有手动测试那一行**

**关键突破——去看 Action log：**

Dashboard 右下角的 Action log 小窗口显示：

```
Time                      User/Recipient  Action        Status
2026-09-05 10:42:45 PM    Admin           local-script  FAILED ❌
2026-09-05 10:42:45 PM    (空)            (空)          No message defined for media type
```

**根因**：Zabbix 7.0 的 Action Operation 编辑弹窗里有个 **`Custom message` 复选框，默认不勾选**。不勾 = 不定义消息模板 = Zabbix 知道"要发通知"，但不知道"发什么内容" → 直接失败。

**修复第一步**：勾选 `Custom message`，填入带宏的 Subject 和 Message（见阶段 F4）。

**修复第二步（更隐蔽）**：配完之后再次触发，日志出现新行但**内容是空的**：

```
2026-09-05 23:20:45 |  |  |
```

说明脚本被调用了（时间戳是新的），但 `$1 $2 $3` 全空。

**根因**：Zabbix 的 Script 媒介，**参数不是自动传的**，必须在 Media type 的 `Script parameters` 里显式声明：

```
{ALERT.SENDTO}
{ALERT.SUBJECT}
{ALERT.MESSAGE}
```

不配 = 一个参数都不传 = 脚本拿到三个空字符串。

**修复后**：

```
2026-09-05 23:33:45 | ARGS[3]: local Problem: Average on sv01 - ... Host: sv01 Severity: Average ...
```

`$#` = 3，内容完整 ✅

**这次排障沉淀的方法论（比结论更重要）：**

```
告警发不出去的三段式排查：
  ① 看 Action log        → Zabbix 有没有尝试发？（区分"没发"和"发了但失败"）
  ② 看 Status 列的红字   → 失败原因（本次是 No message defined）
  ③ 看脚本日志           → 脚本有没有被真正执行
```

> **为什么一开始绕了弯路**：只盯着脚本日志（`/tmp/zabbix-alert.log`），而**脚本日志只能证明"脚本被调了"，不能证明"Zabbix 尝试调了"**。看 Action log 才是直接问 Zabbix"你到底发没发"。
>
> **推广**：排查任何"该发生但没发生"的问题，先找**最接近源头的那个观测点**，而不是最接近结果的那个。

**差点踩到的第五个坑（虽未发生，但思路值得记）**：systemd 的 `PrivateTmp`。

如果 zabbix-server 服务启用了 `PrivateTmp=yes`，它写 `/tmp` 会写进自己的私有临时目录 `/tmp/systemd-private-*-zabbix-server.service-*/tmp/`，从外面 `cat /tmp/xxx` 永远看不到。排查方法：

```bash
systemctl show zabbix-server | grep -i privatetmp
```

绕过：日志路径改用 `/var/log/zabbix/`。

---

## 4. 知识结晶：五颗钉子

**钉子一：告警四要素，缺一不可**
Trigger（何时算问题）→ Action（干什么）→ Media type（什么渠道）→ User Media（发给谁）。任何一个断了都不响。**排查告警不响，就按这四层依次验证。**

**钉子二：告警是状态机，会去重**
`OK → PROBLEM → 持续（不重发）→ OK`。验证告警必须走完整循环"触发 → 恢复 → 再触发"。看到"时间没变"先查 `Age` 列，**持续中的事件不会重新报**，这是设计不是 bug。

**钉子三：任何单一视角的证据都不能推出"能通"，必须端到端验证**
- D16：`nginx -T` 读磁盘文件（还带注释），`ss -tlnp` 看内核真实 socket → 信 `ss`
- D17：`ss` 证明进程在监听，`nc` 证明包能到达 → **两道独立关卡**
- D18：Action log 证明 Zabbix 尝试发了，脚本日志证明脚本被调了 → **两个不同的观测点**

**钉子四：`refused` vs `timeout` 是运维的分水岭**
refused = 有人应门说不在（端口无服务）；timeout = 敲门没人理（防火墙 DROP / 路由不通）。**能说清这个区别，就是"真干过"和"背过题"的差距。**

**钉子五：先跑通再优化，验证阶段不引入外部依赖**
- D16：先跑通 80 端口，再谈改端口
- D18：先用 Script 写文件验证链路，再换 Email/Webhook
共同原则：**出问题时要能立刻定位是哪一环的锅。**

---

## 5. 面试话术（2 分钟版，口述用）

> "我做过一个 Zabbix 监控项目，从零搭建并打通了告警闭环。
>
> 环境是两台 VMware 虚拟机，一台跑 Zabbix Server 7.0 配 MySQL，另一台装 zabbix-agent2 作为被监控端。
>
> 接入过程中遇到一个典型问题：agent 明明在监听 10050，`ss` 也确认了，但 server 连不上报 timed out。我用三层定位法排查——ping 通说明主机在线，`ss` 确认服务在监听，`hostname -I` 确认 IP 没变，最后 `ufw status` 发现是防火墙 active 而且只放行了 22 和 80。放行 10050 之后立刻通了。这里我总结了一个判别方法：refused 是端口没服务，timeout 是包被防火墙丢了。
>
> 告警这块我配的是 Script 媒介，因为验证阶段不想引入 SMTP 这些外部依赖。中间踩了个坑：告警一直发不出来，我先手动跑脚本能写日志，说明脚本没问题；然后看 Action log 发现状态是 FAILED，提示 `No message defined`——原因是 Zabbix 7.0 的 Action 里 Custom message 默认不勾选，等于没定义消息模板。勾上填好宏之后能发了，但脚本收到的参数是空的，因为 Script 媒介的参数要在 Media type 里显式声明 `{ALERT.SENDTO}` 等三个宏。两层都配好之后，告警内容完整送达。
>
> 最后用停 agent 的方式做了完整验证：停掉 → 3 分钟后告警触发 → 恢复 → 再触发，确认告警状态机工作正常。整个链路是服务异常 → agent 失联 → 触发器翻转 → Action 匹配 → 媒介调用脚本 → 宏渲染 → 日志落地。"

### 高频追问 8 条

**Q1：Zabbix 不出数据你怎么排查？**
A：zabbix-get 四连——`nc` 测端口、`agent.ping` 测存活、`agent.hostname` 测身份一致性、`system.uptime` 测取值。身份一致性是头号坑，`Hostname=` 必须和 Web 里填的一字不差。

**Q2：`Connection refused` 和 `timed out` 有什么区别？**
A：refused 是主机可达但端口没服务监听，对方明确回绝；timed out 是包石沉大海，通常是防火墙 DROP 或路由不通。前者查服务，后者查防火墙和网络。

**Q3：告警一直不响，你怎么查？**
A：看 Action log —— 它能区分"Zabbix 没尝试发"和"发了但失败"。Status 列的红字直接给原因。脚本日志只能证明脚本被调了，不能证明 Zabbix 尝试调了。

**Q4：为什么告警要设 3 分钟窗口？**
A：防抖。网络抖动、重启瞬间、交换机闪断都会导致短暂失联，不设窗口会被假告警淹没。3 分钟是及时性和准确性的平衡，核心系统可以缩到 30 秒。

**Q5：同一个问题会不会重复告警？**
A：不会。Zabbix 的触发器是状态机，OK→PROBLEM 只发一次，持续期间不重发，直到恢复后再次触发才发新的。这是告警去重设计，防止告警疲劳。

**Q6：主动模式和被动模式怎么选？**
A：被动（默认）是 server 主动去问 agent，需要 agent 侧开 10050 入站；主动是 agent 主动上报，只需出站。小规模同网段用被动简单直观；大规模、跨 NAT、agent 在防火墙后面时用主动。模板要对应（`Linux by Zabbix agent` vs `... active`）。

**Q7：生产环境用什么告警渠道？**
A：Email 适合低优先级留痕，企业微信/钉钉/飞书 Webhook 适合日常，电话/短信给 Disaster 级。关键是**按严重等级分渠道**，避免所有告警都打手机。Zabbix 可以在 User Media 里用 `Use if severity` 控制。

**Q8：Zabbix 和 Prometheus 你怎么选？**
A：Zabbix 开箱即用、有完整 Web 界面和告警体系、传统企业装机量大、走 agent 和 SNMP，适合物理机和传统架构；Prometheus 是云原生标配，拉模型 + PromQL + K8s 生态好，适合容器和微服务。国内传统企业运维岗 Zabbix 更常见。

---

## 6. 命令速查表

### agent 端（sv01）

| 用途 | 命令 |
|---|---|
| 装 agent2 | `sudo apt install -y zabbix-agent2` |
| 看关键配置 | `grep -E '^Server=|^ServerActive=^Hostname=' /etc/zabbix/zabbix_agent2.conf` |
| 重启 | `sudo systemctl restart zabbix-agent2` |
| 开机自启 | `sudo systemctl enable zabbix-agent2` |
| 服务状态 | `systemctl is-active zabbix-agent2` |
| **看监听（铁证）** | `sudo ss -tlnp \| grep 10050` |
| 防火墙放行 | `sudo ufw allow 10050/tcp` |
| 看防火墙 | `sudo ufw status` |

### server 端（sv02）

| 用途 | 命令 |
|---|---|
| 装 zabbix-get | `sudo apt install -y zabbix-get` |
| 测网络层 | `nc -zv <IP> 10050 -w 5` |
| 测 agent 存活 | `zabbix_get -s <IP> -k agent.ping` |
| **测身份一致性** | `zabbix_get -s <IP> -k agent.hostname` |
| 测取值 | `zabbix_get -s <IP> -k system.uptime` |
| 测 CPU | `zabbix_get -s <IP> -k system.cpu.load` |
| 查库里模板数量 | `mysql -uzabbix -p'密码' -e "SELECT COUNT(*) FROM zabbix.hosts WHERE status=3;"` |
| 看告警脚本 | `cat /usr/lib/zabbix/alertscripts/alert.sh` |
| 手动测脚本 | `sudo -u zabbix /usr/lib/zabbix/alertscripts/alert.sh "a" "b" "c"` |
| 看告警日志 | `cat /tmp/zabbix-alert.log` |
| 查 server 配置 | `sudo grep -i <关键字> /etc/zabbix/zabbix_server.conf` |
| 查 PrivateTmp | `systemctl show zabbix-server \| grep -i privatetmp` |
| 重启 server | `sudo systemctl restart zabbix-server` |

### MySQL 提示（老坑）

- `-p'密码'` **紧贴** = 内联密码（正确）
- `-p 密码` **有空格** = MySQL 把"密码"当数据库名 → `ERROR 1045`
- 屏幕无回显是正常的安全设计，盲打回车即可

### 路径速记

| 路径 | 内容 |
|---|---|
| `/etc/zabbix/` | 所有配置文件（server / agent2 / web） |
| `/var/log/zabbix/` | 日志 |
| `/usr/lib/zabbix/alertscripts/` | 告警脚本 |
| `/usr/share/zabbix/` | Web 前端代码 |

---

## 7. 遗留与下一步

### 今晚收尾动作

- [ ] **sv01 上 `sudo systemctl start zabbix-agent2`**（别让告警一直挂着）
- [ ] 确认 Web 上 sv01 恢复绿色
- [ ] **拍快照**：`D18-完成-告警闭环打通`（sv02）；sv01 也建议拍一个

### D18 未完成部分（明天做）

| 场景 | 命令 | 说明 |
|---|---|---|
| **磁盘爆满** | `dd if=/dev/zero of=/tmp/bigfile bs=1M count=3000` | 造 3GB 大文件，触发磁盘告警。**注意**：需先看 `df -h` 剩余空间，3GB 可能不够触发阈值，可能需要调整触发器阈值或写入更大文件 |
| **CPU 高峰** | `stress --cpu 4 --timeout 300` | 跑 5 分钟，触发负载告警（sv01 是 2 核，模板阈值按核数算） |
| **清理** | `rm -f /tmp/bigfile` | 造完故障必须清理，否则告警一直挂着 |

### 后续

- **D19**：MySQL 备份恢复演练（备份账号 + `mysqldump --all-databases` + 删库恢复）+ Redis 部署
- 补配 Recovery operations 的消息模板，让"已恢复"也发通知
- 考虑禁用 `Number of installed packages has been changed` 这个噪音触发器（装个包就告警）
- 把 Script 媒介升级成真实渠道（企业微信/钉钉 Webhook），这是简历上更亮的一笔
- **D17-D18 的知识要回填进简历项目 2**，重点写"告警闭环"四个字

---

## 附：本日时间线

| 时间 | 事件 |
|---|---|
| 21:55 | D17 阶段 D，Templates 弹窗搜不到 Linux |
| 22:04 | SQL 验证模板数 356，确认数据在库 |
| 22:09 | 只保留 `Linux by Zabbix agent`，保存主机 |
| 22:13 | Availability 红色，报 timed out |
| 22:17 | 三层定位，发现 sv01 的 ufw 只放行 22/80 |
| 22:21 | 放行 10050，nc 测得 succeeded |
| 22:25 | zabbix-get 四连全过，sv01 变绿，**D17 完成** |
| 22:34 | 停 agent，进入 D18 |
| 22:45 | 首次告警触发（PROBLEM） |
| 22:51 | 手动测脚本成功，证明脚本本身没问题 |
| 22:57 | Web 三查全过，锁定时机问题 |
| 23:07 | 发现 Age 24m，确认是持续事件 |
| 23:09 | **Action log 暴露 `No message defined`** |
| 23:12 | 勾选 Custom message，填入宏模板 |
| 23:20 | 再次触发，Action log `Sent`，但参数为空 |
| 23:29 | Media type 补齐三个 `{ALERT.*}` 参数 |
| 23:33 | **告警携带完整内容送达，闭环打通** |
