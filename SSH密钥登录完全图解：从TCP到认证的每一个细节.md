# SSH 密钥登录完全图解：从 TCP 连接到认证的每一个细节

> 配套 D2（SSH 安全登录）。目标：不只"会配"，而是把每一次 ssh 登录背后的机制讲透——这是面试高频题，也是理解所有加密通信的基础。

---

## 一、先建立正确的心智模型：三个角色

| 角色 | 持有物 | 备注 |
|---|---|---|
| **你的电脑**（客户端） | 私钥 `id_ed25519` | 绝不出本地 |
| **网络** | — | **不安全的公共通道，假设有人一直在偷听** |
| **服务器 sv01** | 你的公钥（在 authorized_keys）、服务器主机密钥对 | 公钥可公开 |

**核心假设（安全设计的前提）**：网络是不可信的，所有通信都可能被窃听、被篡改、被冒充。SSH 的每一步设计都在对抗这三件事。

---

## 二、公钥与私钥的本质

### 2.1 三条核心性质

| 性质 | 含义 |
|---|---|
| ① 公钥加密 → 私钥能解 | 别人用你的公钥锁上的东西，只有你能开 |
| ② 私钥签名 → 公钥能验 | 你签的名，任何人拿你的公钥都能验证真伪 |
| ③ **公钥反推私钥不可行** | 单向数学难题，是整个体系的基石 |

### 2.2 两个比喻

**比喻 A · 锁和钥匙**
- 公钥 = 一把能随便复制给别人的锁
- 私钥 = 唯一的钥匙
- 别人用你的锁锁东西，只有你的钥匙能开

**比喻 B · 印章和验钞机（更贴合 SSH 的真实机制）**
- 私钥 = 你的私章（只有你有）
- 公钥 = 验钞机（谁都能拿去验）
- 你盖章后把"盖好的章"（签名）寄出去，对方用验钞机核对
- **关键**：对方拿到的是"盖好的章"，不是你的印章——他造不出第二枚你的章

### 2.3 两个用途（极易混淆）

| 用途 | 方向 | 目的 | 典型场景 |
|---|---|---|---|
| **加密** | 公钥加密 → 私钥解密 | **保密**：只有收件人能看 | PGP 加密邮件、HTTPS 的密钥传输 |
| **签名** | 私钥签名 → 公钥验签 | **证明身份**：确实是本人授权的 | **SSH 登录**、HTTPS 证书、软件发布签名、区块链交易 |

**SSH 用的是"签名"，不是"加密"。**

### 2.4 ⚠️ 一个重要纠正：为什么不是"加密随机数"

很多教程（包括简化讲解）说："服务器用公钥加密一段随机数，客户端用私钥解密后回传。"这个说法**方向对但机制不准确**，准确说法是**签名/验签**。三个理由：

1. **ed25519 是 EdDSA 签名算法，设计上就不能加密**——它只支持签名和验签，没有加密功能
2. **RSA 既能加密也能签名，但 SSH 协议里 RSA 也走签名模式**（算法名 `ssh-rsa`、`rsa-sha2-256`、`rsa-sha2-512` 全是签名算法）
3. **签名能防重放攻击**：签名数据里绑定了本次连接的**会话 ID**。如果是"加密随机数"模式，黑客录下你上次的应答，下次原样重发就能冒充你；用签名的话，录下来的签名换个会话立即失效

> **面试价值**：能讲清"SSH 用签名而非加密、ed25519 不能加密"这个区别，是"真懂"和"背过命令"的分界线。

---

## 三、完整登录过程：七个阶段

### 阶段 1 · TCP 连接与版本协商

TCP 三次握手（SYN → SYN-ACK → ACK）连到服务器的 **22 端口**，然后双方互报版本：

```
服务器：SSH-2.0-OpenSSH_9.6p1 Ubuntu-3ubuntu13
客户端：SSH-2.0-OpenSSH_9.6
```

**为什么必须是 SSH-2**：SSH-1 有设计缺陷（CRC32 补偿攻击漏洞，可被注入数据），2000 年后全面废弃。

### 阶段 2 · 算法协商

双方互发"我会什么"的清单，五类算法：

| 算法类型 | 作用 | 常见值 |
|---|---|---|
| 密钥交换算法 | 怎么商量出共享密钥 | curve25519-sha256 |
| 主机密钥算法 | 服务器用什么密钥证明身份 | ssh-ed25519 |
| 加密算法 | 后续通信怎么加密 | aes256-gcm |
| MAC 算法 | 怎么校验数据没被篡改 | hmac-sha2-256 |
| 压缩算法 | 要不要压缩 | none / zlib |

**匹配规则**：客户端按自己偏好顺序列出，服务器挑**第一个自己也会的**。

### 阶段 3 · 密钥交换（DH/ECDH，最精妙的部分）

**要解决的问题**：在一条有人偷听的网络上，双方怎么商量出一个共享密钥？

**DH 的魔法（颜料比喻，讲给面试官特别好用）**：

1. 双方公开约定一种"**基础颜料**"（公开参数）
2. 各自偷偷选一种"**私密颜料**"（只有自己知道）
3. 各自把基础颜料 + 自己的私密颜料**混合**，把混合结果发给对方
4. 收到对方的混合颜料后，再混入**自己的私密颜料**
5. **结果**：双方得到**完全相同的最终颜色**

偷听者看到了什么？基础颜料、A 的混合颜料、B 的混合颜料。他**无法还原最终颜色**——因为"混合"是单向的，没法把混合后的颜料拆开分离出原来的私密颜料。

**数学保证**：离散对数难题——正向计算容易，反向求解在现有算力下要算几万年。

**产出两样东西**：
- **会话密钥**：后续对称加密用
- **会话 ID**：唯一标识这一次连接（阶段 6 认证要用）

**前向保密（Forward Secrecy，面试加分点）**：
每次连接都生成**临时的**密钥对，连接结束就丢弃，会话密钥**从不存盘**。这意味着：即使将来服务器的私钥被偷，黑客也解不开**过去录下来的**通信记录。这是现代 SSH/HTTPS 的标配。

### 阶段 4 · 服务器身份验证（防中间人）

**问题**：客户端怎么知道对面是真服务器，不是黑客冒充的？

**解法**：服务器装机时自动生成一对**主机密钥**（`/etc/ssh/ssh_host_ed25519_key`）。此时服务器用它对交换数据**签名**发过来，客户端用服务器的**主机公钥**验签。

**你 D2 第一次连时看到的提示，就是这一步**：

```
The authenticity of host '192.168.207.128' can't be established.
ED25519 key fingerprint is SHA256:xxxxxxxxxxxxx.
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

| 情况 | 客户端行为 |
|---|---|
| 首次连接 | 没记录 → 询问，输 `yes` 后把服务器公钥记进 `~/.ssh/known_hosts` |
| 已知主机 | 比对指纹一致 → 直接放行（所以第二次连不再问） |
| 指纹对不上 | 报 `REMOTE HOST IDENTIFICATION HAS CHANGED` 红色警告——可能是中间人攻击，也可能是服务器重装了系统 |

**指纹（fingerprint）是什么**：公钥是一长串字符不好人工比对，对它做哈希生成短字符串（如 `SHA256:xxxx`），像人的指纹一样用于快速核对。

### 阶段 5 · 加密隧道建成

双方用阶段 3 的**会话密钥**做**对称加密**（如 AES-256-GCM）保护后续所有通信。

**为什么这里改用对称加密**：非对称加密慢，比对称加密慢几百上千倍，不适合加密大量数据。所以分工是：
- **非对称**（DH/RSA/ed25519）：用在"商量密钥"和"签名"这类**小数据量**场景
- **对称**（AES）：用在"传输大量数据"（命令、输出结果、文件）

至此隧道建好。**用户名、密码、每条命令全在隧道里**，窃听者只能看到乱码。

### 阶段 6 · 用户认证：密钥验签（核心）

| 步骤 | 谁 | 做什么 |
|---|---|---|
| 1 | 客户端 | 声明：用 jasper 登录、密钥类型 ed25519、公钥指纹 xxx |
| 2 | 服务器 | 查 `/home/jasper/.ssh/authorized_keys`——有没有这个公钥？没有则拒绝 |
| 3 | 客户端 | 构造待签名数据：**会话 ID** + 用户名 + 服务名 + 认证方式 + 公钥 |
| 4 | 客户端 | 用**私钥签名**（在本地完成，私钥不出内存） |
| 5 | 客户端 | 把**签名**发给服务器（**不是私钥**） |
| 6 | 服务器 | 用白名单里的**公钥验签**：通过 → 认证成功 |

**第 3 步为什么必须包含会话 ID**：保证签名只对"这一次连接"有效。黑客录下整个登录过程，下次会话 ID 变了，录下来的签名立刻作废（**防重放攻击**）。

**安全账**：

| 项目 | 内容 |
|---|---|
| 网络上传输的 | 公钥（本来就可公开）+ 签名（无法反推私钥） |
| **从未传输的** | **私钥** |
| 服务器上存的 | 公钥（泄露无害，只能验签、不能伪造签名） |

### 阶段 7 · 会话

认证通过后，客户端请求开"会话通道"，申请 pty（伪终端）、启动 shell。命令在隧道里传过去，结果传回来。断开时 TCP 四次挥手，会话密钥销毁（前向保密）。

---

## 四、关键文件速查

| 文件 | 位置 | 权限 | 作用 |
|---|---|---|---|
| `id_ed25519` | 你的电脑 `~/.ssh/` | 600 | **私钥**，绝不给任何人 |
| `id_ed25519.pub` | 你的电脑 `~/.ssh/` | 644 | **公钥**，可随便公开 |
| `authorized_keys` | 服务器 `~/.ssh/` | 600 | 允许的公钥**白名单** |
| `known_hosts` | 你的电脑 `~/.ssh/` | 644 | 记服务器公钥指纹（防中间人） |
| `ssh_host_ed25519_key` | 服务器 `/etc/ssh/` | 600 | 服务器的**主机私钥**（root 所有） |
| `ssh_host_ed25519_key.pub` | 服务器 `/etc/ssh/` | 644 | 服务器的**主机公钥** |

**注意这里有两组密钥对，别搞混**：
- **你的密钥对**（id_ed25519）：证明"**你是谁**"
- **服务器的主机密钥对**（ssh_host_*）：证明"**服务器是谁**"

**为什么 SSH 对权限如此"洁癖"**（要求 700/600）：如果 `.ssh` 目录或 `authorized_keys` 权限太松（别的普通用户也能写），别人就能把自己的公钥塞进你的白名单，从而登录你的账号。所以 SSH 强制"只有属主能写"，否则拒绝使用密钥——这是安全设计，不是 bug。

---

## 五、常见误解纠正

| 误解 | 正解 |
|---|---|
| "公钥加密随机数，私钥解密" | **不准确**。是私钥**签名**、公钥**验签**；ed25519 根本不能加密 |
| "免密登录不安全" | **恰恰相反**。免密是指不用输密码，换成更强的密钥证明，比密码安全得多 |
| "公钥是秘密，要保管好" | 公钥**本来就可公开**，泄露无害。要保管的是**私钥** |
| "私钥要传到服务器" | **私钥永不上网**。传到服务器的永远是公钥 |
| "known_hosts 只是个缓存" | 它是**防中间人攻击**的关键，指纹变化会直接拒绝连接 |
| "SSH 全程用非对称加密" | 只有密钥交换和签名用非对称，**大量数据用对称加密**（性能考虑） |

---

## 六、面试怎么讲（60 秒版 + 追问预案）

### 60 秒版话术

> "SSH 登录分两大阶段。第一阶段建加密隧道：先 TCP 连到 22 端口协商版本，然后互换算法清单挑出双方都支持的加密套件，接着用 DH 密钥交换在公开网络上算出只有双方知道的会话密钥——这里用临时密钥对，有前向保密，会话密钥不落盘。同时服务器用自己的主机密钥签名，客户端用 known_hosts 里的记录核对，防止中间人冒充。
>
> 第二阶段是身份认证。如果用密钥登录，客户端在本地用私钥对一段数据签名——这段数据包含本次连接的会话 ID，所以签名只对这一次连接有效，能防重放。服务器拿到签名后，用 authorized_keys 里存的公钥验签。整个过程中私钥从未离开本地，网络上只有公钥和签名，截获了也造不出私钥。
>
> 需要纠正一个常见说法：很多人以为 SSH 是'公钥加密随机数、私钥解密'，其实是**签名验签**——ed25519 是 EdDSA 签名算法，设计上就不能用来加密。"

### 追问预案

| 追问 | 答 |
|---|---|
| SSH 和 Telnet 区别 | Telnet 明文传输（密码能被抓包看到），SSH 全程加密；SSH 默认 22 端口，Telnet 23 |
| 为什么用对称加密传数据 | 非对称慢几百倍，不适合大量数据；非对称只用于密钥交换和签名 |
| 什么是前向保密 | 每次连接用临时密钥对，会话密钥不存盘，私钥将来泄露也解不开历史通信 |
| 首次连接的 yes 是干什么 | 确认信任服务器主机公钥，之后记进 known_hosts，下次不再问 |
| 指纹变了报警怎么办 | 可能是中间人攻击，也可能是服务器重装；确认原因后再更新 known_hosts |
| 为什么禁密码登录 | 防暴力破解（机器人每秒试几千次密码）；私钥几乎无法暴力破解 |
| 生产还做什么加固 | 禁 root 直登、改端口、fail2ban 防爆破、VPN/堡垒机跳板、定期轮换密钥 |

---

## 七、现实中的安全加固清单

企业服务器的 SSH 标准做法（按重要性排序）：

1. **禁用密码登录**（你 D2 已做）：`PasswordAuthentication no`
2. **禁止 root 直接登录**：`PermitRootLogin no`
3. **fail2ban**：多次失败自动封 IP，防暴力破解
4. **改端口**（有争议但常见）：减少扫描噪音（真正的安全靠密钥，不靠改端口）
5. **堡垒机/VPN**：SSH 不直接暴露公网，先过跳板机，可审计可回放
6. **密钥轮换**：定期更换密钥对，离职人员公钥及时从 authorized_keys 移除
7. **审计日志**：记录谁在什么时间从哪个 IP 登录、执行了什么命令

---

*到此，你已经把 D2 背后的机制完整走了一遍。下一块（D3 用户与权限）讲的是：服务器上有多个账号时，谁能对什么文件做什么——同样是面试高频区。*
